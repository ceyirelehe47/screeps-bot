# Read Envelope XV · Online I Retry I

这是完整执行包，不是源码实现任务。复用 01205865d975878d04c9993a0c97a6ccab952be5；不增加 source commit。

入口：AGENT-RUN.md。先完整性验证、165 项包测试、只读源码复用与全部真实仓库门禁，再调用唯一 observe。

变化只在执行层：上线前最多 30 分钟、最多 6 轮的有界只读择时；每轮全新校准；全部失败方案保留；身份再核验后取第一个可行方案；绑定与上传均额外要求 3 分钟余量。保持 75 分钟固定观测上限、2 CPU、4 点、100 tick 间隔，以及 candidate/restore 各最多一次、不重试。已绑定后不再择时。

不能满足条件时零上传结束并归档。补丁不存在，materialize 不存在，旧包与旧 marker 不复用。生产恢复和业务完整性分别验收。
