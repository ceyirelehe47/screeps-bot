# XII 独立受控恢复 I — 2026-09-20

本包只把已知候选恢复到已验收的旧生产原始字节，并独立验证运行状态。它不做新的 CPU 实验，不绑定窗口，不创建新的源码提交，不重建生产包。

从 `AGENT-RUN.md` 执行。包为自包含 Node.js 22 工具；不需要 materialize 或 npm install。实际 backup/candidate 快照从原 execution/live/run 读取，绝不内嵌、复制到证据仓库或上传给其他服务。

**新授权边界：**账号所有者转交本任务书执行时，允许在旧实验恢复尝试已经耗尽的基础上，额外进行最多一次恢复 POST。它是新授权，不是删除旧标记、自动重试旧 POST，或新开实验。旧恢复最多 1 次 + 本包新增最多 1 次，总恢复尝试最多 2 次。新候选 POST 为 0。

原实验仍为 `CPU_DIAGNOSTIC_CAPTURE_INCONCLUSIVE`。即使本包恢复成功，也不得改写原始采样验收结论。
