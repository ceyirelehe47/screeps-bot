'use strict';
/** OFFLINE authoring only. IX adds bounded diagnostic boundaries and primitive
 * work counters to the already-audited VII capsule. It does not change table
 * filtering, validation, aggregation, query semantics, caching or admission.
 * Reversibility is provenance; behavioral tests remain authoritative. */
const RULES = [
  ["commitment-entry", "function buildTreasuryCommitmentIndex(options, compatContext) {\n    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r;\n    const tick = options.tick;\n", "function buildTreasuryCommitmentIndex(options, compatContext) {\n    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r;\n    const compatDiagnostics = options.compatDiagnostics;\n    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"commitmentSetup\");\n    let attributionInvalidRegion = \"setup\";\n    let attributionTaskInvalidEvents = 0;\n    let attributionReservationInvalidEvents = 0;\n    let attributionDemandHealthChecks = 0;\n    let attributionReceiverHealthChecks = 0;\n    let attributionRouteCandidates = 0;\n    let attributionOwnerEvaluations = 0;\n    const tick = options.tick;\n"],
  ["invalid-region", "    const recordInvalid = () => {\n        invalidCommitmentRecords.count += 1;\n    };\n    metrics.taskRecords = Object.keys(options.tasks).length;\n", "    const recordInvalid = () => {\n        invalidCommitmentRecords.count += 1;\n        if (attributionInvalidRegion === \"tasks\")\n            attributionTaskInvalidEvents += 1;\n        else if (attributionInvalidRegion === \"reservations\")\n            attributionReservationInvalidEvents += 1;\n    };\n    attributionInvalidRegion = \"tasks\";\n    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"commitmentTasks\");\n    metrics.taskRecords = Object.keys(options.tasks).length;\n"],
  ["demand-health-count", "        const countsTowardDemand = (0, resourceTransferTaskHealth_1.countsResourceTransferTaskTowardDemand)(task, healthOptions);\n", "        attributionDemandHealthChecks += 1;\n        const countsTowardDemand = (0, resourceTransferTaskHealth_1.countsResourceTransferTaskTowardDemand)(task, healthOptions);\n"],
  ["route-count", "        if (task.origin === \"manual\" || countsTowardDemand) {\n            // Canonical first-route-wins order, including manual tasks that do\n", "        if (task.origin === \"manual\" || countsTowardDemand) {\n            attributionRouteCandidates += 1;\n            // Canonical first-route-wins order, including manual tasks that do\n"],
  ["receiver-health-count", "        if ((0, resourceTransferTaskHealth_1.isHealthyReceiverCapacityCommitment)(task, healthOptions.automaticTaskNoProgressTtl)) {\n", "        attributionReceiverHealthChecks += 1;\n        if ((0, resourceTransferTaskHealth_1.isHealthyReceiverCapacityCommitment)(task, healthOptions.automaticTaskNoProgressTtl)) {\n"],
  ["reservation-boundary", "    const reservationSnapshot = [];\n    const reservedByRoomResource = new Map();\n    metrics.reservationRecords = Object.keys(options.reservations).length;\n", "    attributionInvalidRegion = \"reservations\";\n    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"commitmentReservations\");\n    const reservationSnapshot = [];\n    const reservedByRoomResource = new Map();\n    metrics.reservationRecords = Object.keys(options.reservations).length;\n"],
  ["owner-count", "        const expired = entry.expiresAt < tick;\n        const owner = (0, ownerIdentity_1.isValidTreasuryOwnerIdentity)(entry.owner)\n", "        const expired = entry.expiresAt < tick;\n        attributionOwnerEvaluations += 1;\n        const owner = (0, ownerIdentity_1.isValidTreasuryOwnerIdentity)(entry.owner)\n"],
  ["finalize-boundary", "    metrics.invalidCommitmentRecords = invalidCommitmentRecords.count;\n", "    attributionInvalidRegion = \"finalize\";\n    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"commitmentFinalize\");\n    metrics.invalidCommitmentRecords = invalidCommitmentRecords.count;\n"],
  ["commitment-end", "        metrics, };\n    return index;\n}\n", "        metrics, };\n    if (compatDiagnostics) {\n        compatDiagnostics.work({\n            commitmentTaskRecords: metrics.taskRecords,\n            commitmentPendingTaskRecords: metrics.pendingTaskRecords,\n            commitmentTaskInvalidEvents: attributionTaskInvalidEvents,\n            commitmentDemandHealthChecks: attributionDemandHealthChecks,\n            commitmentReceiverHealthChecks: attributionReceiverHealthChecks,\n            commitmentRouteCandidates: attributionRouteCandidates,\n            commitmentReservationRecords: metrics.reservationRecords,\n            commitmentReservationInvalidEvents: attributionReservationInvalidEvents,\n            commitmentOwnerEvaluations: attributionOwnerEvaluations,\n            commitmentActiveReservations: metrics.activeReservationRecords,\n            commitmentScopeBuckets: taskScopes.size,\n            commitmentRoomBuckets: taskRooms.size,\n            commitmentMergeRoutes: mergeIndex.size,\n            commitmentReservationScopes: reservedByRoomResource.size,\n        });\n        compatDiagnostics.boundary();\n    }\n    return index;\n}\n"],
  ["observation-entry", "function buildTreasuryObservation(options) {\n    var _a, _b, _c, _d, _e;\n    const epoch = {\n", "function buildTreasuryObservation(options) {\n    var _a, _b, _c, _d, _e;\n    const compatDiagnostics = options.compatDiagnostics;\n    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"observationSetup\");\n    let attributionExistingLocations = 0;\n    let attributionResourceKeys = 0;\n    const epoch = {\n"],
  ["observation-rooms-boundary", "    const rooms = [];\n    const empireTotals = {};\n    for (const room of options.rooms) {\n", "    const rooms = [];\n    const empireTotals = {};\n    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"observationRooms\");\n    for (const room of options.rooms) {\n"],
  ["observation-counts", "        const storageScan = scanLocation((_a = room.storage) !== null && _a !== void 0 ? _a : undefined);\n        const terminalScan = scanLocation((_b = room.terminal) !== null && _b !== void 0 ? _b : undefined);\n", "        const storageScan = scanLocation((_a = room.storage) !== null && _a !== void 0 ? _a : undefined);\n        const terminalScan = scanLocation((_b = room.terminal) !== null && _b !== void 0 ? _b : undefined);\n        attributionExistingLocations += (storageScan.exists ? 1 : 0) + (terminalScan.exists ? 1 : 0);\n        attributionResourceKeys += storageScan.nonZeroKeys + terminalScan.nonZeroKeys;\n"],
  ["observation-finalize", "    const data = Object.freeze({\n", "    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"observationFinalize\");\n    const data = Object.freeze({\n"],
  ["observation-end", "    return createObservationView(data);\n}\n", "    if (compatDiagnostics)\n        compatDiagnostics.boundary(\"observationView\");\n    const view = createObservationView(data);\n    if (compatDiagnostics) {\n        compatDiagnostics.work({\n            observationRooms: options.rooms.length,\n            observationLocations: options.rooms.length * 2,\n            observationExistingLocations: attributionExistingLocations,\n            observationResourceKeys: attributionResourceKeys,\n        });\n        compatDiagnostics.boundary();\n    }\n    return view;\n}\n"],
];
function check(ok, code) { if (!ok) { const e = new Error(code); e.code = code; throw e; } }
function substitute(text, before, after, name) {
  check(text.split(before).length === 2, 'ATTRIBUTION_TRANSFORM_COUNT:' + name);
  return text.replace(before, () => after);
}
function transform(text) {
  let result = text;
  for (const [name, before, after] of RULES) result = substitute(result, before, after, name);
  check(restore(result) === text, 'ATTRIBUTION_TRANSFORM_NOT_REVERSIBLE');
  return result;
}
function restore(text) {
  for (const [name, before, after] of [...RULES].reverse()) text = substitute(text, after, before, name);
  return text;
}
module.exports = { transform, restore, rules: RULES.map(([name]) => ({ name, replacements: 1 })) };
