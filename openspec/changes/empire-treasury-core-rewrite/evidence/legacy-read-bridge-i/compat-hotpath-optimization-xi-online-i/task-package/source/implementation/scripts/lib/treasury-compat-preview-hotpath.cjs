'use strict';
/** XI: deterministic preview hot-path transform; business output is unchanged. */
const RULES = [
  ["direct-selected-total","function direct(room: Room | undefined, kind: Kind, resources: readonly string[]): { status: string; value?: Direct } {\n  try {\n    if (!room) return { status: \"not_visible\" };\n    if (room.controller?.my !== true) return { status: \"not_owned\" };\n    const s = room[kind];\n    if (!s) return { status: \"absent\" };\n    if (s.my !== true || !str(s.id, 96)) return { status: \"unreadable\" };\n    // Endpoint-local reference only: no value or Store is retained across calls.\n    // Core observation still obtains its own Store and independently enumerates it.\n    const store = s.store;\n    if (!store || typeof s.isActive !== \"function\") return { status: \"unreadable\" };\n    const used = store.getUsedCapacity(), free = store.getFreeCapacity(), capacity = store.getCapacity();\n    const active = s.isActive(), cooldown = kind === \"terminal\" ? (s as StructureTerminal).cooldown : null;\n    // Capacity reduction may leave inventory above capacity. Preserve signed\n    // free or a saturated zero, rather than inventing capacity=used+free.\n    if (!nonneg(used) || !nonneg(capacity) || !Number.isSafeInteger(free)\n      || !(used + free === capacity || (used > capacity && free === 0))\n      || typeof active !== \"boolean\" || (kind === \"terminal\" && !nonneg(cooldown))) return { status: \"unreadable\" };\n    const amounts: Record<string, number> = Object.create(null);\n    for (const r of resources) {\n      const a = store.getUsedCapacity(r as ResourceConstant);\n      if (!nonneg(a) || a > used) return { status: \"unreadable\" };\n      amounts[r] = a;\n    }\n    if (Object.values(amounts).reduce((a, b) => a + b, 0) > used) return { status: \"unreadable\" };\n    return { status: \"ok\", value: { id: s.id, used, free, capacity, overCapacity: used > capacity,\n      active, cooldown, amounts } };\n  } catch { return { status: \"unreadable\" }; }\n}\n","function direct(room: Room | undefined, kind: Kind, resources: readonly string[]): { status: string; value?: Direct } {\n  try {\n    if (!room) return { status: \"not_visible\" };\n    if (room.controller?.my !== true) return { status: \"not_owned\" };\n    const s = room[kind];\n    if (!s) return { status: \"absent\" };\n    if (s.my !== true || !str(s.id, 96)) return { status: \"unreadable\" };\n    // Endpoint-local reference only: no value or Store is retained across calls.\n    // Core observation still obtains its own Store and independently enumerates it.\n    const store = s.store;\n    if (!store || typeof s.isActive !== \"function\") return { status: \"unreadable\" };\n    const used = store.getUsedCapacity(), free = store.getFreeCapacity(), capacity = store.getCapacity();\n    const active = s.isActive(), cooldown = kind === \"terminal\" ? (s as StructureTerminal).cooldown : null;\n    // Capacity reduction may leave inventory above capacity. Preserve signed\n    // free or a saturated zero, rather than inventing capacity=used+free.\n    if (!nonneg(used) || !nonneg(capacity) || !Number.isSafeInteger(free)\n      || !(used + free === capacity || (used > capacity && free === 0))\n      || typeof active !== \"boolean\" || (kind === \"terminal\" && !nonneg(cooldown))) return { status: \"unreadable\" };\n    const amounts: Record<string, number> = Object.create(null);\n    let selectedTotal = 0;\n    for (const r of resources) {\n      const a = store.getUsedCapacity(r as ResourceConstant);\n      if (!nonneg(a) || a > used) return { status: \"unreadable\" };\n      amounts[r] = a;\n      selectedTotal += a;\n    }\n    if (selectedTotal > used) return { status: \"unreadable\" };\n    return { status: \"ok\", value: { id: s.id, used, free, capacity, overCapacity: used > capacity,\n      active, cooldown, amounts } };\n  } catch { return { status: \"unreadable\" }; }\n}\n"],
  ["resource-catalog","      if (ports.shard() !== cfg.shardName) return { status: \"wrong_shard\" };\n      if (!cfg.resources.every(r => ports.resources().includes(r))) throw new Error(\"invalid resource\");\n","      if (ports.shard() !== cfg.shardName) return { status: \"wrong_shard\" };\n      const availableResources = ports.resources();\n      if (!cfg.resources.every(r => availableResources.includes(r))) throw new Error(\"invalid resource\");\n"],
  ["valid-room-names-decl","      const validRooms: Room[] = [];\n      const next: Previous = { tick, endpoints: Object.create(null) };\n","      const validRooms: Room[] = [];\n      const validRoomNames: string[] = [];\n      const next: Previous = { tick, endpoints: Object.create(null) };\n"],
  ["valid-room-push","        if (safe && room && room.controller?.my === true) validRooms.push(room);\n","        if (safe && room && room.controller?.my === true) { validRooms.push(room); validRoomNames.push(name); }\n"],
  ["included-set","      const included = new Set(validRooms.map(r => r.name));\n","      const included = new Set(validRoomNames);\n"],
  ["projection-names","        const projectionRowsPlanned = validRooms.length * cfg.resources.length;\n        builderDiagnostics?.boundary(\"projectionRows\");\n        for (const name of validRooms.map(r => r.name)) for (const resource of cfg.resources) {\n","        const projectionRowsPlanned = validRoomNames.length * cfg.resources.length;\n        builderDiagnostics?.boundary(\"projectionRows\");\n        for (const name of validRoomNames) for (const resource of cfg.resources) {\n"],
  ["core-observation-names","      report.coreObservationRooms = validRooms.filter(() => !!observations).map(r => r.name);\n","      report.coreObservationRooms = observations ? [...validRoomNames] : [];\n"],
];
function check(ok, code) { if (!ok) { const e = new Error(code); e.code = code; throw e; } }
function substitute(text, before, after, name) { check(text.split(before).length === 2, 'PREVIEW_HOTPATH_TRANSFORM_COUNT:' + name); return text.replace(before, () => after); }
function transform(text) { let result = text; for (const [name,before,after] of RULES) result = substitute(result,before,after,name); check(restore(result) === text, 'PREVIEW_HOTPATH_TRANSFORM_NOT_REVERSIBLE'); return result; }
function restore(text) { for (const [name,before,after] of [...RULES].reverse()) text = substitute(text,after,before,name); return text; }
module.exports = { transform, restore, rules: RULES.map(([name]) => ({ name, replacements: 1 })) };
