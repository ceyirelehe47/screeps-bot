# Patch mirror

`0001-online-x-retry-I-package-runtime.patch` is a Git-generated mirror of the executable-package changes from the superseded Online X v2 package to Retry I. It includes the bounded read-only retry implementation, current-baseline workflow, tests and task documents.

The patch is not applied to the production repository: the task package already contains the complete executable implementation. `INTEGRITY.json`, this patch directory and maker validation files are intentionally outside the mirror to avoid self-reference.
