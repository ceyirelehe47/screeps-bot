# 接续关系与边界

本包从已验收 CPU I 源码和离线验证继续，不取代或改写 CPU I 的原始证据，也不再执行 v1/v2/v3 收尾。0003 仍是1 raw / 0 complete，0003 后续恢复已闭合。

被替代的是后续实验不得再沿用的旧正式观察行为：只接受 sampled、缺失十六进制实体解码、解码失败即切断安全收尾流、恢复依赖 game/time，以及以旧 collector 承担恢复运行确认。

本包实现：单层实体解码；四点CPU诊断与业务完整性分离；真实 completeness 字符串验证；无 time/overview 依赖的有界恢复读取；原运行目录共用一次性标记；新的恢复专用流与独立原始日志验证。

不授权12点正式观察0004，不提高 maxSampleCpu=2、reserveCpu=5，不跳过过期窗口提前量，不重试窗口。后续是否优化或扩大实测范围，须先审查本轮真实数据。
