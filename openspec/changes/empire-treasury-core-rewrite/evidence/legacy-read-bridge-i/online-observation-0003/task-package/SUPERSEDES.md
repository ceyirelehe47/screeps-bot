# 接续关系与历史保留

本轮从 refactor `ee03fb4a871b1a707a4244d2f3a7819e5982324d` 与 compat `fcfc125f0e5cd6c3370318b4fee0450f4d09af53` 接续。

不废止、不改写 Guard Joint Probe II 的原始运行与已通过的独立重裁决；不再执行旧的 heartbeat 295 行门禁、不重新运行25分钟探针。旧失败结果、历史误入库完整快照披露和已披露流程偏差保持原件。

本包是新的正式兼容观察0003，不复用0002已过期的104e47b窗口，也不把旧探针session误作正式上传session。执行时唯一绑定一个新窗口；无论成功、缺样本还是未知结果，都不在同一任务重绑第二窗口。

旧宽泛任务中“每点完整CPU均可读”“存在独立builder计数器”等表达不作为本轮虚构的验收字段。按冻结读取器实际支持的字段验收，末点完整CPU不可观测；本轮范围见 AGENT-RUN §6。

此前两分支最终通过的内容不需要重做。0003只允许新的配置ON/OFF线性提交与新evidence目录；禁止更改或覆盖任一旧实验原件。
