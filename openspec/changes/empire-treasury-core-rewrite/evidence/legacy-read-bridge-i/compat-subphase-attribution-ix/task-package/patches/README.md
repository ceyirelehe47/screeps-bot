# Patch mirror

The patch is a review/reproduction mirror of the fixed implementation payload. The Agent application tool writes the exact payload bytes; do not apply the patch a second time after `tools/run.cjs apply`.
