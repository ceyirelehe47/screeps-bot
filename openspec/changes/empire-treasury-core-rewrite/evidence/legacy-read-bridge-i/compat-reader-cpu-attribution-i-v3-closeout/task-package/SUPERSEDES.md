# v3取代范围与缺陷归属

v2 ZIP SHA-256：`0af7f4fcc759262ba9ea1adcb1cd0de816244e41f4cbda26ffe78683319ebf3f`。

制包方复现了冲突：原`0001-reader-cpu-accounting.patch`第73、78行各含单个空格，作为unified diff空上下文有效；将该文件原字节加入Git索引时，普通`diff --cached --check`在本轮检查策略下返回2。v2步骤17要求固定字节、步骤18又不允许这两处，不能同时成功。原生产源码补丁仍可应用，这不是已证明的CPU功能回归。

v3只取代步骤18及其后的收尾指令。v2源码、helper、测试和既有执行结果继续作为原始交付证据；不把归档里的旧包伪装成执行过v3。也不将Agent尚未推送的本地完成状态宣称为已经远端验收。

本次选择精确语法例外，而不是回头剪掉旧patch的两个字节：这样可以保留实际执行包与已暂存77文件的身份。原native结果2永久保留，另加明确有界授权；其他whitespace问题不在授权内。

`patches/0001-step18-check-mirror.patch`仅供代码审查，展示旧任务书单行门禁应如何替换。不得应用到旧任务包或旧归档；实际执行入口和完整实现已经在本v3包中。
