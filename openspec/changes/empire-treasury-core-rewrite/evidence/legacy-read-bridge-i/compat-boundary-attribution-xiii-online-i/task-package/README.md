# Boundary Attribution XIII + Online I

Self-contained Node.js 22 execution package for a fixed diagnostic-only source change. No materialization and no package dependency installation are required. The existing Screeps repository must already have its pinned dependencies for repository gates and Rollup.

Start with AGENT-RUN.md. The package includes the complete 12-path source patch, exact source identities and expected default-OFF tree, 48 execution-tool tests, and the complete read/observe/restore/verify/archive/publish workflow. The source patch adds 42 tests to the existing 290 repository Node cases (332 total).

Purpose: partition the unexplained outer commitment interval, not declare its cause or optimize away its measured cost. The bridge retains the 2 CPU cooperative budget, adds two bounded call probes, and locally disables subsequent sampling after observing more than 5 CPU. It cannot preempt the current synchronous call or restore production code by itself.

Use a new execution directory. This authorization permits at most one candidate upload and one canonical-backup restore. No write retries, new fifth sample, second window, third-party overwrite, or Treasury production cutover. Previous XII incident recovery quotas remain consumed and are not reused.
