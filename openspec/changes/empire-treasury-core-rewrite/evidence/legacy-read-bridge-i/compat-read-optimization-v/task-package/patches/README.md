# Patch mirror

This is a native Git-generated diff from the locked pre-V files to all12 implementation files. Tools apply the exact payload transactionally; do not apply the patch a second time. The patch is for independent check/apply byte verification. No whitespace exceptions, no automatic whitespace repair.
