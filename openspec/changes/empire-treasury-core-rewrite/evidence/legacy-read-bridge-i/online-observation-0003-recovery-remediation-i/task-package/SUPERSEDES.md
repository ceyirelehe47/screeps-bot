# 取代关系与未闭合项

本包不改写任务包 `screeps-compat-observation-0003-2026-09-12.zip`（SHA-256 `3fc8a713a577484600aaface97831ca04efbb9ba1369bdd373787a9c1660802d`），也不改写它的历史执行记录。旧正式 observe 入口不再使用。

针对真实证据中的缺陷，本包新增恢复专用执行链，避免再把已经退出的旧 collector 当作恢复后运行确认来源；恢复不依赖旧绝对 tick 窗口，不读取无关的 game time / overview 才决定是否能够恢复。

解码补充十六进制 HTML 实体，但原样本验收保持严格。`BRIDGE_JSON_INVALID` 的历史记录保留；回放产物单独说明首点其实是一条合法、但 CPU 预算不足的 partial 报告。

未完成且本轮不授权的事项：CPU 冷启动/稳态分阶段测量、预算优化或上调、原正式采集器的通用失败 drain 模式、下一次正式窗口。恢复专用通路通过不等于未来正式上传链已经全部通过。

禁止把恢复专用 75 秒日志补入 0003 的 12 点样本集合。即便恢复完全成功，0003 仍为 INCONCLUSIVE。
