# engine-run/——真实引擎实验证据（占位）

状态：**AUTHORIZATION_REQUIRED**——真实引擎实验 NOT_RUN：两个探针与新增
main 均未上传、未装载到任何游戏世界、未武装、未调用真实 terminal.send。

本目录在授权后才会包含：世界初始化快照、只读基线样本、T 前控制记录、
原始用户 console/服务器日志、调用 tick 同步结果、后续逐 tick 样本、判读
表与最终控制记录。

在此之前本目录保持为空（本 README 是唯一内容）。
