# Tasks

## 1. 阶段 A：OpenSpec 与迁移地图

- [x] 1.1 建立 `openspec/changes/empire-treasury-rearchitecture/`（proposal/design/tasks/specs）
- [x] 1.2 记录权威数据边界、职责边界、tick/contract 生命周期、18 条不变量落实位置
- [x] 1.3 迁移地图（8 个消费者顺序）+ 旧模块删除清单 + adapter 删除条件

## 2. 阶段 B：Treasury Core

- [x] 2.1 `types.ts`：LocationKind/Epoch/JournalEntry/QueryContext/BalanceView/Metrics
- [x] 2.2 `observation.ts`：不可变稀疏 observation（storage+terminal）、多方向索引、observedAtTick/epochSeq、fresh scope 独立构建；复用 TickContext myRooms；每 Store 单次 Object.keys；无 Game 引用保留；无 Memory 持久化
- [x] 2.3 `projection.ts`：transaction journal、幂等结算（stale epoch 拒绝/already_settled 拒绝）、projected overlay、reconciler（差异计数+样本，不静默）
- [x] 2.4 `facade.ts`：TreasuryService（observation/commitments/query/recordAcceptedAction/metrics/resetForTest）+ RuntimeServices 注册（无新 global 槽）
- [x] 2.5 带上下文查询：observed/projected/committed/spendable/incoming/outgoing/transferable；spendable 非负 + overcommitted 置位；无无上下文 available API

## 3. 阶段 C：承诺统一索引

- [x] 3.1 transfer tasks：outgoing/pendingOutgoing/incoming/pendingIncoming/taskCount/route merge lookup（复用 canonical 谓词，零复制持久数据）
- [x] 3.2 production reservations：活跃聚合、holder 存在性（孤儿计数）、过期排除（不删除原始记录）
- [x] 3.3 receiver commitments：healthy incoming 聚合 + 轻量 headroom（口径差异在 design.md 声明）
- [x] 3.4 后续（非本轮）：ReceiverCapacityLedger 内存实例并入、Budget Service、Contract Service（authorize 语义）

## 4. 阶段 D：Shadow 与兼容

- [x] 4.1 `shadow.ts`：Treasury vs empireInventoryIndex core + 独立直读双通道对比；覆盖房间 storage/terminal 资源、帝国总量、capacity、缺失位置、stale epoch、承诺重复计数
- [x] 4.2 零行为写入（无 intent API、无生产行为变化）；低频采样（40 tick）+ force 通道；mismatch 环形缓冲 cap + 聚合计数；低频快照 Memory.runtime.treasuryPerf（断言写入，沿用 inventoryPerf 先例）
- [x] 4.3 挂载 main.ts treasuryShadow phase（empireInventoryShadow 之后）
- [x] 4.4 兼容层：本轮直接替换 productionMonitor（无 adapter）；hubProgress adapter 待消费者迁移阶段建立时再定义删除条件

## 5. 阶段 E：消费者迁移

- [x] 5.1 productionMonitor storage/terminal energy 读取改走 Treasury observation（行为输出等价）
- [ ] 5.2 （下阶段）hubProgress/console 面板迁移 + 新旧输出 shadow 对比
- [ ] 5.3 （下阶段）resourceControl 热路径替换（getStoredResources / 重复采集点）

## 6. 验证与门禁

- [x] 6.1 新增测试：observation 不可变/稀疏枚举/总量=Σ桶/查询无副作用；projected 不改 observed/成功才产生 delta/幂等/跨 tick 不重复结算；承诺过期排除/孤儿计数/headroom 非负/spendable 非负/stale epoch 拒绝；shadow 零写；RuntimeServices reset
- [x] 6.2 确定性操作计数断言（storeEnumerations/resourceKeysEnumerated/roomFindCalls=0）
- [x] 6.3 `npm run typecheck`、`npm run build`、全量 Jest 通过（区分本次回归 vs 既有失败）
- [x] 6.4 Jest 预算治理：collect → apply-budget → verify 锚点更新 → `npm run test:budget`
- [x] 6.5 市场安全相关既有测试保持通过（零市场代码改动）

## 7. 审查修复轮（第二迭代：架构阻断项）

- [x] 7.1 显式 tick 生命周期：`beginTick()`/`endTick()` API + main.ts 固定挂载（treasuryBeginTick 在一切业务前 / treasuryEndTick 在 treasuryShadow 后 flush 前）+ phase 契约 39→41；懒初始化保留为安全兜底（零写、计数 lifecycleLazyInitializations）
- [x] 7.2 原子 transaction journal：单腿 action 升级为多 posting transaction（recordAcceptedTransaction 权威入口 + recordAcceptedAction convenience）；全 posting 先验证后一次性写入（零部分写入）；同 transaction 内同 key 先合并再验证
- [x] 7.3 幂等 receipt 持久化：Memory.runtime.treasury.receipts（version/retention 5000 tick/cap 4096/当前 tick 保护/版本不兼容冷启动）；heap 本 tick 缓存 + Memory 跨 tick 与 global reset 权威；>512 单 tick 洪峰无淘汰
- [x] 7.4 decision epoch 绑定：epoch 注册表（shared 1 + fresh N/每 tick 清空）；登记必携 decision 上下文并校验（stale/unknown/scope_mismatch 拒绝，幂等优先）；无绕过 Gateway 的登记入口（convenience 同样强制）
- [x] 7.5 reconciliation 完整性：previous-finals ∪ current-observed key 并集；分类 inflow/outflow/new_resource/new_location/new_room/room_lost/location_lost/structure_replaced；tick gap 与 global reset 显式标记；mismatch 样本携带 transactionId/kind 追溯（上一 tick journal 有界副本）
- [x] 7.6 commitment 点时快照：primitive 化（无 task/reservation 对象引用）；route merge 预构建索引（零线性扫描）；receiver healthy incoming 构建期预聚合（不回扫 live store）；revision invalidation（bumpTreasuryCommitmentRevision + 架构边界测试守护全部 mutation 入口）
- [x] 7.7 owner-aware 查询：TreasuryQueryOwner（holderId+scope）；自身 reservation 排除、其他 owner 保留、无 owner 保守、非法 fail closed（spendable=0/overcommitted）
- [x] 7.8 projected capacity 与输入验证：projectedUsed/FreeCapacity；transaction 验证（transactionId 格式/kind/source/posting delta 整数非零/RESOURCES_ALL/位置存在/金额非负/容量不越界）；receiver headroom observed+projected 双轨
- [x] 7.9 resetForTest 修复：journal/overlay/heap 幂等缓存/previousJournal/metrics 全清；只读快照冻结副本（journalSnapshot/reservationSnapshot/samples）；持久状态经 clearTreasuryPersistenceForTest 显式清理
- [x] 7.10 metrics 与 Memory 类型化：全部计数器接线（含 commitmentIndexQueries/生命周期/epoch 拒绝/receipt 清理）；Memory.runtime.treasury（receipts/lifecycle）与 treasuryPerf 正式声明 + memoryDeclarationBoundaries 指纹更新
- [x] 7.11 测试补齐：lifecycle（幂等/无消费者/tick_closed/缺 end 补救/gap/reset/冷启动/懒初始化）、transaction（两腿/三腿/中间非法回滚/NaN/容量/位置/格式/单 tick 601 笔/跨 tick/global reset 重放）、epoch（shared/fresh/stale×2/scope 混用/unknown/Facade 级）、reconciliation（key 并集全分类/追溯/有界）、commitments（快照不可变/revision 失效/零写/projected headroom）；新增 treasuryLifecycle.test.ts 与 treasuryCommitmentInvalidationBoundaries.test.ts

## 8. 上线门槛（后续阶段，非本轮）

- [ ] 8.1 Treasury shadow 连续 1000+ tick 零 mismatch 后迁移 resourceControl 热路径
- [ ] 8.2 market-fresh epoch 接入前独立安全评审（不破坏 fresh floor/双读/CAS/WAL/permit）
- [ ] 8.3 旧模块按删除清单分批退役，每批附 shadow 证据

## 9. 第三轮数据安全修复（P1：receipt 驱逐 / fresh 基线 / receiver 缓存）

- [x] 9.1 receipt 安全驱逐契约重做：store 升级 version 2（settled key "t:"+transactionId 前缀编码防原型污染 + entryCount 计数）；只自动回收 retention 过期条目，未过期条目绝不因容量驱逐；admission 预检（写入任何状态前）满容且无过期可回收时拒绝 receipt_capacity_exhausted（独立指标，零部分写入；已结算 id 仍优先 already_settled）；满容 admission 低频回收（O(1) 快路径）；v1→v2 无损迁移（只执行一次+指标）；未知版本/entryCount 损坏 fail closed（原数据保留、持续拒绝、有界诊断）
- [x] 9.2 decision epoch 绑定 exact observation：epoch 注册表条目保存该 epoch 的 exact immutable observation 引用（heap-only 每 tick 清空）；recordAcceptedTransaction 物理验证（位置/数量/容量/incarnation）使用 decision 指向的观察，不回退 shared；overlay 跨 epoch 共享防同 tick 超卖（代码注释固定三层语义）；endTick 后 beginFreshObservation 返回 null
- [x] 9.3 receiver projected headroom 实时化：projected 字段每次查询动态组合（静态承诺 + observed 容量 + 当前 overlay 容量聚合），不缓存依赖 overlay 的旧结果；locationCapacityDelta 改按位置 capacityDeltas 聚合 map（commit 原子更新/endTick/reset 清空/查询 O(1)）；单调 projectionRevision 暴露（缓存失效与诊断）
- [x] 9.4 空结构/空房间两层对账：endTick 归档 room/location manifest（owned 房间集合、exists/structureId/容量事实）；reconcile 拆 manifest 结构层（new_room/room_lost/new_location/location_lost/structure_replaced，每位置至多一条）+ 资源 key union 层（inflow/outflow/new_resource 独立计数）；样本 dimension 标记结构/资源维度
- [x] 9.5 owner 验证强化：TreasuryQueryOwner 增加 roomName；deps.resolveHolderRoom 运行时解析 holder 存在性与归属房间；声明不一致/不存在 fail closed；多房间查询只在其合法归属房间排除自己；不再接受"任意非空字符串即可排除"
- [x] 9.6 查询输入 fail-closed 规范化：非法资源/非法或重复房间/非法或重复位置/NaN/Infinity/负 withhold → 保守全零视图（contextStatus=invalid_fail_closed）+ queryInvalidContexts 计数；合法路径 contextStatus=valid
- [x] 9.7 receipt key 编码与 transactionId 语义：settled 一律 "t:" 前缀编码（__proto__/constructor 等合法 id 字面量恒为普通自有键）；新增 formatTreasuryStableTransactionId（无 tick 前缀，跨 tick 重试恒同 id，receipt 幂等跨 tick 生效）；tick 前缀 helper 文档限定为无重试语义的新动作
- [x] 9.8 权威 mutation 覆盖补全：migrateResourceTransferTasksToV2（legacy schema 迁移改写任务）通知 commitment revision 失效；route merge 同 route 重复 key 恢复"第一个匹配"（旧 findMergeablePendingTask 语义）
- [x] 9.9 Memory 类型与治理联动：runtime.d.ts receipts 类型升级 v2（前缀键/entryCount 注释含驱逐契约与 fail-closed 规则）；memoryDeclarationBoundaries 指纹重算；metrics 新增 receiptCapacityRejections/receiptStoreMigrationsExecuted/receiptStoreIncompatibleFailures/receiptsCorruptedEvicted/queryInvalidContexts（treasuryPerf 快照自动含新字段）
- [x] 9.10 测试补齐：receipt admission（满容拒绝/最老保护/零部分写入/过期恢复/601 笔/reset 恢复/v1 迁移/未知版本/entryCount 损坏/__proto__ 防护）；fresh exact 绑定（低基线拒/小容量拒/journal 保留 scope/双 fresh 各自验证/overlay 防超卖/endTick 后拒/old-unknown-scope 拒）；receiver 实时性（同 tick transaction 后立即减少/observed 不变/流出恢复/多资源聚合/索引不重建）；manifest 两层（空房间/空 terminal/structureId 替换每位置一次）；owner（不存在/房间不符/多房间归属/无预留正常）；query 输入（非法资源/重复房间位置/NaN withhold）；稳定 id 跨 tick 幂等；merge 第一个匹配；性能边界架构断言（admission O(1)/capacityDelta O(1)/exact observation 不回退）

## 10. 第四轮 Pre-Write Hardening（receipt v3 / 两阶段协议 / typed holder）

- [x] 10.1 v1 迁移碰撞修复：v1 裸键 raw key 原样作为 transactionId 输入安全编码（绝不 decode——`abc` 与 `t:abc` 不碰撞）；迁移在临时结构完成全部校验（格式/value 有效性/编码碰撞防御）+ 自检（own key 数/entryCount/存储键格式/每个 settled tick）后一次性原子替换原 store；发现非法 key/value/碰撞时原 store 不变并 fail closed；v1/v2→v3 双链迁移（v2 补 nextExpiryTick）、只执行一次
- [x] 10.2 损坏 value fail closed：settled tick 须为 [0, Game.time] 安全整数（NaN/±Inf/非整数/非安全/负数/未来 tick 判损坏）；迁移不跳过、cleanup 不删除、admission 整体阻断（receipt_store_incompatible）；已可靠识别的旧 id 查询仍 already_settled；损坏 id 阻断不乐观放行；cleanup 只删完整验证且超 retention 的正常条目
- [x] 10.3 过期调度元数据：store 升级 version 3（+nextExpiryTick=min(settledAt)+retention+1，空表 null）；未到点 beginTick 零扫描、满容 admission O(1) 拒绝不反复全表扫描；到点一次清理并重算；global reset 后 load 完整验证（entryCount/键格式/value/元数据一致性，损坏 fail closed 不放宽容量）；插入/迁移/清理后元数据恒一致
- [x] 10.4 确定性操作计数：receiptFullScans/admissionFastPaths/admissionFullStoreBlocked/expiryCleanupScans/storeIncompatible/migrations/slotsRemaining/nextExpiryTick（metrics 聚合 + treasuryPerf 快照）；可运行次数断言测试（满载重试零扫描/beginTick 未到点零扫描/到点一次扫描）
- [x] 10.5 两阶段 transaction 协议：prepareTransaction（Game 动作前完成全部验证 + receipt 槽位预留，pending 有界 64 且其余 admission 计入预留数）→ abort（零状态释放）/commit（重验物理后原子写入；prepare_invalidated 零写入）；commit 兑现不因容量/兼容性被拒；handle tick 内有效（endTick/beginTick 作废）；重复 prepare 幂等/重复 commit already_settled/已 commit 不可 abort；单阶段入口保留
- [x] 10.6 typed holder 解析：新建 holderResolution（`nuker:` 内嵌 objectId / `synthesis:` owned 房间归属 / 裸 Game object id 三形态统一解析，返回 {kind, roomName}）；commitments 默认 holderExists 走 typed 解析（logical holder 不再被误判 orphan 导致 committed 低估）；TreasuryQueryOwner 增加必填 holderKind 且须与运行时解析一致（冒充其他类型 fail closed）
- [x] 10.7 stable id 铸造成分防碰撞：kind 与每个 discriminator 校验为不含冒号的 [A-Za-z0-9_\-.]{1,64} token（数字须非负安全整数、至少一个 discriminator），tuple 边界碰撞（("a:b","c") vs ("a","b:c")）/空串/NaN/非法字符一律抛错；tick 前缀 helper 同样校验
- [x] 10.8 查询 fail-closed 补全：非管辖（unknown/unowned）房间拒绝（管辖集合=注入房间源）；空 rooms/locations scope 拒绝；allowProjected/allowIncoming/subtractOutgoing/subtractReservations 非布尔真值（0/"true"）拒绝
- [x] 10.9 验证补全：delta 非零安全整数；同 key 合并溢出拒绝；净零腿剔除 + 全抵消 no-op transaction 整笔拒绝（no_op_transaction）；金额/容量结果安全整数校验；epochSeq 单点递增（每发行恰好 +1 无空洞）；fresh 每 tick 上限 8（超限 null + freshEpochLimitRejections，下一 tick 恢复）
- [x] 10.10 Memory 类型与治理联动：runtime.d.ts receipts 升级 v3（+nextExpiryTick，注释含迁移链/损坏规则/调度语义）；memoryDeclarationBoundaries 指纹重算；metrics 新增 12 字段（receipt 计数 8 项 + fresh 上限 + 两阶段 3 项，treasuryPerf 快照自动含）
- [x] 10.11 测试补齐：receipt v3（v1 碰撞/危险字面量无损、v2→v3、v1 损坏 fail closed、v1 非法 key、v3 value 损坏可靠 id 仍幂等、元数据损坏、满载 O(1) 重试零扫描、到点一次扫描）；两阶段（成功兑现/abort 零状态/槽位预留他人填满仍成功/prepare_invalidated 零写入/跨 tick 失效/endTick 作废/already_finalized/重复 prepare 幂等）；typed owner（logical 排除自己/kind 冒充拒/holderResolution 默认解析）；query 补全（非管辖房间/空 scope/非布尔）；no-op/2^53/合并溢出；formatter 边界（碰撞 throw/合法往返）；fresh 上限与 epochSeq 连续性；commitments 默认解析（logical 不 orphan）

## 11. 第五轮 Write-Admission Correctness（tentative ledger / opaque handle / staged commit / typed owner / completeness）

- [x] 11.1 canonical transaction identity：transactionId 铸造 v2——canonical tuple 序列化（类型标签 s/n + 长度前缀，number 42 与 string "42"、元组边界、字段顺序、attempt sequence 全部可区分；Unicode/空格/冒号/空串为合法业务字段）+ 双 lane FNV-1a 稳定 hash 定长输出；stable（ts1_）与 per-tick（tt1_&lt;tick&gt;_）分命名空间不可碰撞；validator 不变（存量 receipt 键完全兼容）；固定 test vectors 锚定
- [x] 11.2 canonical snapshot + payload digest：prepare 载荷深复制 + postings 规范排序 + 逐层冻结（调用方 prepare 后修改原 input 不影响 canonical）；digest 覆盖 identity/kind/source/decision epoch/全部 canonical postings；相同 ID 相同 digest 重复 prepare 幂等返回同一 handle、不同 digest 返回 prepare_conflict
- [x] 11.3 tentative ledger：prepare 成功预留每 (room,location,resource) 净资源 delta + 每位置净容量 delta + receipt 槽；后续 prepare 与单阶段登记的授权计算计入全部 tentative（同一 handle 自身预留不重复计算）；tentative 不进入 public projected（gauge 只读诊断）；abort 原子释放、commit 转 committed
- [x] 11.4 opaque prepared handle：冻结 capability 对象 + 服务实例私有 WeakSet 对象身份验证（伪造对象/JSON round-trip 副本/跨 service generation 一律 invalid_handle）；tick 自校验（不依赖调用方先 beginTick）；状态机 prepared/executing/committing/committed/aborted/faulted/expired（commit 一次、abort 一次、terminal 不回退）
- [x] 11.5 commit 语义重做：tentative → committed 兑现，不做业务 admission——Game API 已 OK 后不再因资源/容量/receipt 条件拒绝；prepare_invalidated 正常路径删除（他笔 transaction 推进 overlay 不再使 commit 失败——tentative 预留保证不变量）
- [x] 11.6 staged commit + write fault：可预期失败前置 prepare；commit 拆 receipt 发布（commitSettledReceipt 返回 written/already_settled/fatal）→ heap 发布（journal/overlay 分段故障钩子）→ handle 状态三段；任一阶段意外失败 → faulted 终态（不当作普通 rejected/aborted）+ tentative/槽不释放 + 最小有界持久 write-fault marker（Memory.runtime.treasury.writeFault）+ 全部后续 writer fail closed（write_admission_locked）；global reset 后凭 marker 仍可发现；只有显式 clearTreasuryWriteFaultForRepair 解除；故障注入测试覆盖 receipt 前/receipt 后 heap 前/journal/overlay/handle 五个故障点（无静默半提交）
- [x] 11.7 endTick outstanding 审计：未决 prepared 计数 + 有界样本（transactionId/digest/preparedAtTick/kind/source，上限 8）+ leak 指标；executing 状态视为严重异常（write-fault marker + 全局锁）；普通 prepared tick 边界失效释放但留审计事件；绝不静默当作正常 abort；下一 tick 旧 handle 不可用
- [x] 11.8 安全执行包装器 executePreparedAction：prepare → Game API 恰好一次 → ok=true commit / ok=false abort / 抛错 abort+rethrow；prepare 失败不执行 callback；正常完整执行后 outstanding 恒为 0；生产 writer 唯一推荐入口
- [x] 11.9 typed reservation owner：持久 typed identity（game-object/logical-service/task/contract/legacy-unresolved + id/roomName/namespace/lifecycleRef），entry 平铺字段与 store key 不变（marketSaleProtectionAdapter 完全兼容）；版本化迁移 migrateResourceReservationsForTypedOwner（幂等标记/数值不动/bump revision/损坏计入 damaged 不乐观忽略）；typed mutation（reserve/renew/release ForOwner）为唯一新写入口，旧字符串入口降级 deprecated adapter；调用方迁移（nukerControl typed 预留释放、factory/synthesis 自排除 typed 化——仅 identity 表达，业务行为不变）
- [x] 11.10 保守占用与 owner-aware 查询：无法确证 owner 失效的预留（active-unresolved/missing-owner）全额计入 committed——只有 expiresAt 或显式 release 解除；orphan 降级为诊断分类 ownerStatus；自排除用完整 typed identity 比较（同字符串不同 kind/namespace 不互相排除）；legacy-unresolved 不允许被普通 owner declaration 排除；TreasuryQueryOwner 升级 ownerKind/ownerId/namespace
- [x] 11.11 commitment 验证与 completeness：task（status/resource/房间/amount/remaining≤amount/origin/tick/blocked）与 reservation（room/resource/amount/expiresAt/holder/owner）记录级验证；损坏不进聚合（负 amount 不抬高 spendable、NaN 不污染）、不删原数据；能定位 bucket 标 (room,resource) scope incomplete、否则 globally-incomplete；balance view 暴露 commitmentStatus 与 authorizationSafe（incomplete scope 的 spendable=0/overcommitted=true）；unrelated scope 保持 complete；owner 缺失但数值合法继续全额扣除（missing ≠ incomplete）；shadow 通道 3 同口径
- [x] 11.12 单阶段入口退役：recordAcceptedTransaction/recordAcceptedAction 移出 TreasuryService 公共 API；实现保留经 treasury/compat 兼容模块访问（仅供既有测试与迁移过渡）；架构边界测试守护 9 个生产 writer 模块不得引用单阶段/compat、故障注入器与修复入口仅测试可引用、reservation store 直接写入仅限 resourceReservation.ts
- [x] 11.13 查询与 receipt 指标补全：withhold 收紧非负安全整数；rooms/locations 查询入口防御性快照；receipt 扫描计数细化（entriesVisited/migrationScans/loadValidationEntries/expiryCleanupEntries/fatalInspectionEntries）；到期清理与 nextExpiry 重算合并单次遍历；fatal-store 巡检不再有未计数 Object.keys 全扫
- [x] 11.14 测试补齐：canonical vectors（类型/边界/顺序/attempt/Unicode/超长定长/跨 tick 稳定）；tentative ledger（100k 双 prepare 拒/abort 后可行/容量超配/跨资源聚合/tentative 不入 projected/abort 只释放自己/不相关并行/gauge 一致/单阶段不绕过）；opaque handle（伪造/JSON/跨 generation/跨 tick 未 begin/重复幂等/conflict/状态机终态）；safe wrapper（prepare 失败不调用/非 OK abort/抛错 abort+rethrow/恰好一次/outstanding=0/执行期占用）；fault injection（五故障点无静默半提交/锁/global reset 恢复/显式修复）；endTick 审计（outstanding 样本/executing 严重故障/下 tick 失效）；typed owner（持久化/迁移/幂等/损坏保留/保守扣除/同字符串不同 kind/logical 精确排除/legacy 不可排除）；completeness（负 amount/NaN/remaining&gt;amount/非法资源/incomplete scope 零 spendable/unrelated complete/零写入）；架构边界 5 项；性能 fixture（64 并行 prepare 零扫描/多桶授权/abort-commit 清理/4096 admission O(1)/512+3 commitment/重复 query 零重建）
> **第五轮边界声明（未完成项，不得视为已完成）**：ResourceControl/terminal/lab/factory/market writer 迁移；ReceiverCapacityLedger 全量整合；Budget/完整 Contract Service；1000 tick shadow 与 CPU canary 完成；旧系统删除。当前 Treasury 写路径零生产接入，真实 writer 仍待独立评审后经 executePreparedAction 接入

## 12. 第六轮 Fault Recovery & Authority Integrity

- [x] 12.1 typed owner 持久 key：ownerToken（kind 前缀 + logical-service namespace 段 + id）与 makeReservationStoreKey 收敛唯一权威；store key v3 编码完整 identity；同 id 不同 kind/namespace 共存与独立 release；外部模块不得自行拼接持久 key（架构测试）
- [x] 12.2 key 迁移 v3 原子化：临时结构全量验证（形状/legacy key 一致性/新 key 无碰撞）→ 一次性引用切换 + version=3 + bump revision；malformed/collision 终止且不部分覆盖、不推进版本、授权 fail closed；幂等可重复；测试覆盖成功/重复/中途 collision/中途 malformed/失败后数据不变
- [x] 12.3 durable quarantine：Memory.runtime.treasury.quarantine（q: 前缀键、64 条上限 + overflowed）；endTick/beginTick 补救对 executing/faulted 先落 quarantine 再释放 heap tentative；普通 prepared 正常 expire；跨 tick/跨 service generation/global reset 测试；同 id prepare 拒绝（transaction_quarantined）；query 授权计入流出占用、projectedFreeCapacity 扣减
- [x] 12.4 safe execute 结果语义：executed_unsettled（Game OK + commit fault，携带 actionResult/transactionId/digest/faultPhase/retryForbidden）；executed_abort_failed（Game 非 OK + abort 未确认）；callback 抛错 abort 确认后 rethrow、abort 未确认转 faulted+quarantine（abort_failed）后 rethrow；同 id 下次调用 callback 零计数
- [x] 12.5 显式 fault resolution：移除 clearTreasuryWriteFaultForRepair；faultResolution 模块 resolve-as-committed（补 receipt 幂等/清 quarantine/清匹配 marker/global reset 后可完成/防重放）与 resolve-as-not-executed（仅 executing_at_end_tick/abort_failed 允许；释放 quarantine、不写 receipt、返回 reprepareAllowed）；错误 id/digest/不允许 resolution 拒绝且 fault 不动；生产 tick 不可自动调用（架构测试）
- [x] 12.6 receipt corruption fail closed：commitSettledReceipt 对 corrupted 返回 fatal（不 already_settled、不发布 heap projection）；prepare 前 corruption callback 零调用；Game OK 后 corruption → durable fault/quarantine；prepare 后人为损坏 receipt 再 commit 的确定性测试
- [x] 12.7 handle 生命周期有界：WeakSet/WeakMap 承载防伪与全生命周期记录；active strong registry 只含未清理状态，commit/abort 即删、tick 边界 stub 化（丢 canonical/observation/shape 引用）；preparedActive=active registry 大小；压力测试（大量终态后回到 0、terminal handle 幂等可用）
- [x] 12.8 runtime input 验证前置：公开 writer 入口在 canonicalize/digest 前形状验证（input/postings/decision/数值）；malformed → 结构化 rejected（invalid_input）不 throw；零 tentative/零槽位/零 registry 污染/callback 零调用
- [x] 12.9 commitment completeness 补严：status/blockedReason 枚举校验；resource ∈ RESOURCES_ALL；聚合累加安全整数检查（溢出→scope incomplete）；无法定位 scope 的损坏→globally incomplete；receiver 房间 completeness 判定修复（\u0000 分隔解析）
- [x] 12.10 authorizationSafe 重定义：多条件联合（commitment complete + receipt healthy + 无 write fault + 无 unresolved quarantine + lifecycle open + service/tick 合法 + migration 完成）；authorizationBlockers 有界诊断；新条件不归零数量字段；clean/fault/corruption/closed/migration/quarantine 全场景测试
- [x] 12.11 端到端场景测试：14 项（同 id 双 owner 隔离/普通 prepared 跨 tick 释放/executing 跨 tick quarantine/global reset 后拒再执行/Game OK+commit fault 全链/resolve-as-committed 幂等/resolve-as-not-executed 释放/不允许 resolution 拒绝/corruption 非 already settled/malformed input 结构化拒绝/终态 handle 不增长/枚举损坏 fail closed/故障阻断 authorizationSafe=false/性能不退化）
- [x] 12.12 验证与预算：typecheck/build/全量 Jest/verify-jest-budget（新预算锚点，requiredBaselineCommit 指向含全部实现与测试的前置 commit）；evidence/round6-fault-recovery-local-validation.md 记录起始 HEAD/最终 commit/验证命令与真实结果；不部署、不接真实 writer

## 13. 第七轮 Quarantine Closure & Schema Activation

- [x] 13.1 全局 quarantine write blocker：prepare 在同 id quarantine 检查后增加全局阻断（任意 unresolved/overflow → quarantine_write_blocked、store 损坏 → quarantine_store_fatal），callback 零调用；marker 清除后仍有其它 quarantine 时持续阻断；A/B 双 quarantine 逐个解决测试
- [x] 13.2 quarantine fault-slot 预留：持久 entryCount + active handle 数 < 64 才允许 prepare（O(1)），满则在 callback 前拒（quarantine_capacity_exhausted）；commit/abort/expire 释放 slot；fault 原子转换 slot 为持久 entry；legacy overflowed fail closed + 显式 repair（faultResolution 内，生产禁调）
- [x] 13.3 quarantine 版本化权威（schema v1）：version/entryCount 元数据 + 全字段 shape validation（key 编码/transactionId/digest/枚举/安全整数/聚合溢出）；global reset 后首次 load 全量验证 + heap health cache + 损坏 fail closed（prepare 阻断/resolution 拒绝/原数据不动）；write-fault marker 严格 shape validation（损坏视为存在 fault）；单一 canonical deltas（容量由其派生，删除 capacityDeltas 双权威）
- [x] 13.4 quarantine 资源/容量保守口径：正净流入减少 free capacity（projectedFreeCapacity + commitments capacityDelta 回调统一，receiver headroom 同口径）；负流出不增加 free capacity；正资源 delta 不加 spendable；多资源净额聚合；聚合 revision 缓存（query 不全扫）
- [x] 13.5 callback throw = execution unknown：抛错不再 abort——立即 faulted + marker（phase=action_threw_execution_unknown + 有界异常诊断）+ durable quarantine + 锁定后 rethrow；同 id 再执行 callback 零调用；ok:false + abort 失败立即隔离（phase=action_returned_non_ok_abort_failed）；phase 枚举拆分（commit 类 vs execution-unknown 类）
- [x] 13.6 post-observation fault resolution：输入加 evidence（conclusion/observationTick/source）与 guard（activeTransactionIds/currentObservationTick，facade 提供）；active handle 检查、tick/observation 时序检查、evidence 匹配、still_uncertain 保持隔离；not-executed 仅 execution-unknown 类 phase；resolve-as-committed 以 resolution tick 写 receipt（5001+ tick 延迟测试）+ action tick 保留于 resolution tombstone（有界 256 + retention 惰性清理 + 幂等 already_resolved）；resolution 后 endTick 不重新 quarantine
- [x] 13.7 reservation schema activation gate（v4）：ensureReservationSchemaActivated（空店初始化/legacy 迁移/失败拒绝/corrupted 拒绝）；挂载 beginTick bootstrap + 每个 mutation 入口自检；memoryCleanup 保留幂等兜底；无混合 store 测试
- [x] 13.8 canonical owner identity v4：长度前缀 token（ow2:<kindCode>:<nsLen>:<ns><id>）消除字段边界歧义；identity=kind+namespace+id（roomName/lifecycleRef 不参与）；kind-specific 收紧（ls namespace 必填、非 ls 禁止）；migration v1/v2/v3→v4（entry.owner 权威、碰撞检测、原子替换）；固定 test vectors（冒号边界/同 id 不同 kind/不同 namespace/Unicode/空格/超长）
- [x] 13.9 reservation mutation 权威：结构化结果（room/resource/amount/ttl/expiresAt 溢出/owner/gate 全验证，非法与失败零写入，bump 只在实际 mutation 且单次）；deprecated adapter 不二次 bump；listProductionReservations 冻结深拷贝；gc 损坏 entry 不删除、置持久 corrupted 标志 + 显式 repair
- [x] 13.10 write admission readiness：query 返回 writeAdmission {ready, blockers}（context/owner/commitment/migration/corrupted/receipt health+slot/quarantine health+slot/unresolved/fault/lifecycle/tick 全条件）；数值字段不受影响；prepare 独立复查；clean/满载/slot 不足/unresolved/migration/corruption 全场景测试
- [x] 13.11 指标与性能：quarantine entries/slots reserved/slots remaining/store healthy/admission rejections/unresolved count；resolution committed/not-executed/uncertain/rejected；execution unknown count；reservation activation/mutation rejections；prepare blocker 与 slot admission O(1) 的 operation-count fixture
- [x] 13.12 架构边界扩展：生产模块不得 import faultResolution（既有）；reservation mutation 必须经 schema gate；quarantine store 不得由 facade 外部直接写入；ForRepair 入口生产禁调；无条件 clear fault 不得回归
- [x] 13.13 验证与预算：typecheck/build/全量 Jest/verify-jest-budget（新预算锚点）；evidence/round7-quarantine-schema-local-validation.md；不部署、不接任何真实 writer

## 14. 第八轮 Durable Intent & Authorization Binding

- [x] 14.1 durable intent 权威（intents.ts schema v1）：版本化 store（version/entryCount/key="i:"+id/容量 64/entry 全形状/canonical postings/phase 枚举/安全整数与聚合溢出/global reset 首次 load 全量验证/heap health cache/损坏与未知版本 fail closed/显式 repair 并入 faultResolution）；phase 状态机（authorized/ready/executing/returned_non_ok/ok_pending_commit/committed/aborted/execution_unknown/quarantined/resolution_pending——ready 与 executing 绝不混同）；写入失败 callback 零调用 + 槽位释放 + 结构化拒绝
- [x] 14.2 slot 统一计数：recoverySlots = quarantine + intent + 无 intent 的 active handle 恒等式；prepare admission O(1)；intent 接管/fault 转换/commit-abort 回收全路径守恒（指标 intentSlotsRemaining 证明）
- [x] 14.3 global reset 恢复：beginTick 显式分支加载验证；未完成 intent 阻断新 writer；ready 确认未执行关闭；其余保守转 execution-unknown quarantine；恢复幂等；quarantine 写失败时 intent 保留 postings/占用/slot（emergency intent authority）；query 与 readiness 同时计入 quarantine 与 unresolved intents
- [x] 14.4 资源授权（authorization.ts）：结构化授权请求（actionKind/resource/rooms/amount/owner/withhold|policyFingerprint/allowIncoming 默认 false/必扣 outgoing+reservations/容量需求）；授权计算（observation+overlay−outgoing−reservations−quarantine/intent 风险−withhold−其它授权占用 + completeness/store health/readiness/容量/安全整数）；opaque token（heap-only/冻结/WeakSet/单次使用/tick+generation 有界/绑定 epoch+commitmentRevision+projectionRevision+quarantineRevision+ownerKey+policyFingerprint+scope+digest）；授权即占 budget 防超卖；失效矩阵测试（task/reservation mutation、projection commit、quarantine revision、owner、policy、epoch、跨 tick、跨 service、重复消费、伪造）
- [x] 14.5 action contract + adapter registry（actionContracts.ts）：contract 冻结结构（canonical args 确定性派生 postings/structureIds/authorizationDigest/epoch 绑定）；adapter 契约（validate/derivePostings/execute/reconcile）与注册边界（仅 actionContracts.ts 与测试）；测试 adapter（多 posting fixture + 副作用计数 + 可配置 reconciler）；executeTreasuryActionContract 唯一生产入口（授权消费→intent→adapter 恰好一次→commit/abort）；executePreparedAction 降级 test-only（架构测试守护生产禁用）
- [x] 14.6 quarantine per-transaction 保守聚合：Σ max(0,−net) 流出 / Σ max(0,net) 容量（跨 transaction 不抵消、transaction 内先合并）；每步安全整数（溢出 unhealthy + readiness false）；unresolved intents 并入同一口径；快照封闭（entry/list/聚合/health 冻结副本，不泄漏内部 Map）；写入前重验 entry
- [x] 14.7 staged atomic resolution：resolution store v2（version/entryCount/完整 shape/load 验证/未知版本与损坏 fail closed/容量满先于状态变化拒绝/v1 无损升级）；staged 流程（slot 预检→resolving tombstone→结算动作→finalize marker+tombstone）；各阶段故障注入 + global reset 幂等恢复（不重复结算/不丢占用/不错误解锁）；not-executed 先写 final tombstone 再释放
- [x] 14.8 既有 receipt 刷新：refreshSettledReceiptForResolution（own key 存在且有效时更新 settled tick 至 resolution tick；entryCount/updatedAt/nextExpiryTick 同步重算；receiptRefreshes 计数）；统一 replay horizon（heap receipt→Memory receipt→committed tombstone 窗口，同一 retention 常量；receipt 过期 + tombstone 在窗口内时 prepare 仍拒）
- [x] 14.9 reconciliation capability：service 签发 API（intent/quarantine 存在 + 注册 reconciler + active handle 不存在 + post-fault observation + generation/tick）；capability 绑定（transaction/digest/actionKind/conclusion/postFaultEpoch/incarnation/reconcilerKind+Version/generation/tick）+ WeakSet 防伪 + 单次使用；resolution 两函数只接受 capability（evidence/guard 自由对象入口移除）；uncertain 保持隔离；global reset 由新 service 重新签发
- [x] 14.10 owner identity 统一 + reservation store 完整健康：treasuryOwnerIdentityKey 复用 canonical token 算法（移除 NUL 比较键；聚合/排除/mutation/migration 同一 key）；validateReservationStoreHealth 完整 load validation + heap cache/revision；version=4 ≠ healthy；mutation 顺序（输入验证→schema gate→store 健康→写入）；只读 API 冻结快照 + 旧兼容读取不传播 NaN
- [x] 14.11 capacity view 分立：strictProjectedUsedCapacity/strictProjectedFreeCapacity（used+free=physical）与 riskAdjustedFreeCapacity/riskAdjustedReceiverHeadroom 分立；旧名称 deprecated 兼容标注（projectedFreeCapacity→risk-adjusted、receiver projected headroom 语义标注）；receiver admission 走 risk-adjusted
- [x] 14.12 指标与性能：authorization issued/rejected/invalidated/active、adapter mismatch、revision mismatch、intent count/slots/recoveries/conversions/store healthy/write failures、resolution in-progress/faulted/recovered、receipt refreshes、capability issued/rejected、reservation store healthy、risk-adjusted lookups；operation-count fixture（authorize/prepare 不全扫、intent admission O(1)、token 验证 O(1)）；容量模型入 design/evidence（receipt/intent/quarantine/resolution/authorization heap 上限与满载 fail-closed/恢复条件）
- [x] 14.13 架构边界扩展：生产 writer 候选禁 executePreparedAction 任意 callback/compat/直接 prepareTransaction/自行构造 postings（唯一生产入口 = authorization→contract→注册 adapter）；adapter 注册仅 actionContracts.ts+测试；capability/reconciler 注册边界；intent store 直写仅 intents.ts
- [x] 14.14 验证与预算：typecheck/build/treasury 定向/全量 Jest/verify-jest-budget（新预算锚点，requiredBaselineCommit 指向含全部实现与测试的前置 commit）；evidence/round8-durable-intent-authorization-local-validation.md（起始 HEAD/最终 commit/状态机/覆盖与不能覆盖的 failure model/新增 Memory 字段与上限/命令/结果/未部署声明）；不部署、不接任何真实 Game writer

## 15. 第九轮 Contract-Bound Authority & Recovery Closure

- [x] 15.1 安全 canonical contract encoding（canonicalEncoding.ts）：canonicalize（长度前缀编码 + key 排序确定 + 数组保序）先于 validate/derivePostings/execute（三者观察同一 canonical frozen args）；拒绝 cyclic/getter/setter/自定义 prototype/class instance/function/symbol/bigint/undefined/NaN/±Infinity/稀疏数组/非普通对象（结构化拒绝零抛出；getter 不被多次副作用读取）；{a:undefined}≠{}、NaN≠null 无静默碰撞；深度与编码长度有界；digest = AC2 前缀绑定 encoding version + action kind + adapter version + transactionId + canonical args + canonical postings + 结构身份；调用方构建后修改原 args 不影响 postings/digest/执行参数
- [x] 15.2 adapter version binding + 完整结构 incarnation 验证：adapter 接口加 version 强绑定（contract 构建/授权 token/执行/capability 四处一致校验）；structureIds 任意字符串接口替换为受控 structureBindings(args)→{roomName, locationKind, label?}[]；contract 快照覆盖 posting locations + 全部声明结构；执行前逐结构重新验证（disappeared/replaced/kind mismatch → callback 零调用、授权零消费、tentative 不变）；fresh observation 必需——配额耗尽拒绝执行（不退回 shared）
- [x] 15.3 contract-first authorization：authorizeTreasuryActionContract（contract 驱动；actionKind/rooms/locations/每资源 amount/contractDigest 全部从 canonical postings 派生；contractDigest 不可选；token 绑定 adapter version）；授权要求 write admission ready；自由字符串 policyFingerprint 通道移除（policy authority 仅受控 withhold 数值）；authorizeResourceUse 降级 @internal test-only（架构测试守护）；原子签发 bundle（任一资源失败时已签发 token 预算回滚）
- [x] 15.4 原子 bundle redemption：executeTreasuryActionContract 全量只读预验证（对象身份/generation/tick/revisions/contract identity+digest/action kind/adapter version/transactionId/重复 token/exact posting coverage/每资源累计 amount/receiver capacity/policy）→ 结构校验（fresh）→ prepare（tentative 接管）→ 经 execution options 的 redeem hook 在 callback 前一次性消费 → intent → callback；任一失败零消费、budget 不变、callback 零调用；prepare 拒绝不留"授权已消费但 tentative 未接管"窗口
- [x] 15.5 writer kernel 封闭：executePreparedAction/prepareTransaction/consumeTreasuryAuthorization/authorizeResourceUse 从公共生产面收敛（service 对象保留 @internal；execution options 窄接口供 actionContracts）；架构测试升级为扫描全部生产 TypeScript 源码（新增模块自动受约束；白名单仅 treasury 协议实现文件）
- [x] 15.6 intent 完整合同身份（intents.ts v2）：entry 绑定 contractId/contractDigest/actionKind/adapterVersion/authorizationDigest/结构 incarnation/有界 durable payload（adapter.durableFacts 版本化 ≤512，不持久化完整 args/observation）；v1→v2 无损升级 + load 全量验证 + runtime.d.ts 同步
- [x] 15.7 严格 phase 状态机：transitionTreasuryIntentPhase 验证期望前序 + digest/contract 一致（幂等仅限同 identity 且已处目标 phase）；合法迁移表（ready→executing→{returned_non_ok|ok_pending_commit}→execution_unknown 升级；quarantined/resolution_pending 受限进入；committed/aborted 终态）；not_found/store fatal/非法前序/identity mismatch 一律 callback 零调用（修复 not_found 被忽略的缺陷）；写入后 read-back 验证；callback 后 phase 写失败不普通 commit/abort（OK→executed_unsettled、非 OK→executed_abort_failed、抛错→execution_unknown），全部失败路径检查并处置 mark 结果
- [x] 15.8 recovery 事实等级：ready→确认未执行释放；executing/execution_unknown→execution-unknown quarantine；returned_non_ok→action_returned_non_ok_abort_failed（保留"Game 已返回非 OK"）；ok_pending_commit→新增 commit 类 phase ok_pending_commit_unresolved（永不 not-executed resolution——事实单调性）；committed/aborted 终态残留幂等释放；emergency intent 保留原 phase
- [x] 15.9 unified unresolved authority：resolveTreasuryUnresolvedAuthority（quarantine 优先 + intent emergency 兜底；同 id 双存在时 digest/postings/kind 不一致 → fail closed）；capability 签发/prevalidate/resolution/recovery/release 同一 authority 逻辑；intent-only 可签发 capability、resolve-as-committed、phase 允许时 resolve-as-not-executed（ok_pending_commit 拒绝）、still_uncertain 保持占用；resolution 成功幂等释放 quarantine+intent+marker；release 失败保持可恢复
- [x] 15.10 私有 reconciliation capability：registry 移入 service 闭包（reconciliation.ts 只留类型——register/validate/consume 不再导出）；resolve 函数签名接收当前 service（generation 由 service 校验，移除调用者提交的 serviceGeneration）；capability 扩展绑定 authorityKind/contractId+digest/adapterVersion/durable payload version/结构 incarnation；reconciler 输入完整 contract-specific durable facts（移除 postings[0].resource 与单一负数汇总）
- [x] 15.11 staged receipt refresh recovery 修复：resolving committed 恢复只在 receipt tick ≥ tombstone settledAtTick 时 finalize（旧 action tick receipt 不误判）；未刷新时幂等续做 refresh 至原定 settledAtTick（不缩短 replay horizon）或保留 resolving；refresh/finalize 中断可幂等恢复；确定性测试复现"旧 receipt→resolving→refresh 前 reset→recovery 不误 finalize"
- [x] 15.12 resolving tombstone retention：仅 stage=final 可按 retention 清理；resolving 永不被 GC/容量压力驱逐（满载 fail closed）；超龄 resolving 保持并进入诊断 blocker；final not-executed recovery 补完成 quarantine release + intent release + marker 清除
- [x] 15.13 测试与架构：contract/authorization（A 授权不执行 B、digest/adapter version/transactionId/postings 超范围、policy fingerprint 无 authority、第 N token 无效前 N−1 不消费、重复 token、incarnation mismatch 零消费、fresh 耗尽零 callback、prepare 拒绝预算原子、负 posting 全覆盖）；intent（not_found/前序非法/read-back 不一致零调用、OK 写失败不普通 commit、非 OK 写失败不普通 abort、抛错保留权威、recovery 等级映射、phase 幂等仅同 identity）；authority/capability（intent-only 签发与 resolve、ok_pending_commit 拒绝 not-executed、still_uncertain 保留、双权威不一致 fail closed、伪造/JSON round-trip/generation 绕过/跨 service/contract 不匹配拒绝、公开注册不可得）；resolution（旧 receipt 误判防护、refresh 后 finalize 中断恢复、resolving 超龄不删、final 按 retention 清理、满载不驱逐、幂等、stale receipt 不缩短 horizon、receipt fatal 不释放 authority）；canonical（key 顺序同 digest、不同语义异 digest、cyclic/getter/undefined/NaN/Infinity/function/symbol/class 拒绝、adapter 三函数同一 canonical args）；operation-count（bundle 预验证与 token/posting 数线性、授权/prepare 不全表扫描）
- [x] 15.14 验证与预算：typecheck/build/treasury 定向/全量 Jest/verify-jest-budget（新 apply-budget 脚本 + requiredBaselineCommit 指向含全部实现与测试的前置 commit）；evidence/round9-contract-bound-authority-recovery-local-validation.md；真实 writer 零接入确认（八个生产 writer 文件 diff 为空）；不部署、不合并 main

## 16. 第十轮 Durable Authority Cohesion & Bundle Atomicity

- [x] 16.1 拆分 execution outcome 与 settlement state：intent v3 正交二元组、outcome 单调迁移表、旧 phase 保守迁移（未知值 fail closed）、恢复分级改造、facade 执行分支重写（callback 抛错/OK 后故障/非 OK 后故障全部只改 settlement）
- [x] 16.2 quarantine v2 完整合同事实：entry 字段扩展（contract/adapter/durable/bundle/owner/policy/structure facts/outcome/settlement）、事实转移协议（读回验证后才释放 intent）、recovery slot 按 ID 并集去重、v1 原子迁移（并存 intent 合并、legacy 标记、未知版本 fail closed）、runtime.d.ts 与声明边界指纹更新
- [x] 16.3 unified unresolved authority 升级：quarantine v2 为主、双权威身份校验扩展到合同字段、adapter version 演进防护
- [x] 16.4 opaque authorization bundle：类型收缩为不透明句柄、facade 闭包 bundleRegistry、cohort 原子校验（同 owner/policy/epoch/revision）、executeTreasuryActionContract 只接受 opaque bundle
- [x] 16.5 批量原子 redemption：staged ledger change 构造、一次发布、六类注入点（first/middle/last leg、budget publish 前、tentative handoff 前、bundle state 前）、前缀回滚与 internal_authorization_fault marker、重复 redeem 拒绝
- [x] 16.6 writer kernel 封闭：kernelChannel（unique symbol、non-enumerable 挂载）、公共 TreasuryService 类型移除低层方法、testHarness.ts 测试专用视图、架构测试全量扫描强化（kernel symbol/testHarness/低层方法引用）
- [x] 16.7 contract digest AC3：绑定 durablePayloadVersion/durable payload hash/reconciliation contract version、durable facts 变化 → digest 变化、固定 test vector
- [x] 16.8 intent 完整 identity：authorizationDigest/ownerIdentity/policyIdentity 持久化、already_present 完整元组比较、intent_conflict fail closed、read-back 全字段验证、低层 test path 旧 intent 不被 production contract 接管
- [x] 16.9 service-private resolution：resolve 入口成为 service 方法、capability 消费移至 staged resolution 写入后、contract-backed authority 全字段强匹配（弱 optional 检查删除）、跨 reset 仅凭 staged state 恢复、resolution 入口不被生产 tick 自动调用
- [x] 16.10 Treasury-owned policy authority：policyAuthority.ts 注册制 resolver、production 授权不接受调用方 withhold、bundle 绑定 policy identity/version/digest、policy 变化使旧 bundle 失效、无 resolver fail closed、emergency override 显式可审计
- [x] 16.11 统一 write readiness：evaluateTreasuryWriteReadiness 单一评估器（一套 blocker 枚举/优先级/状态来源）、query 视图/contract authorization/prepare 复查三处共用、prepare 独立复查防 TOCTOU
- [x] 16.12 structure binding 权威：受控 identity 判别联合（governed_location/game_object）、label 仅诊断、重合 binding 同 identity 合并/异 identity 拒绝、required structure 构建时必须存在、执行前 incarnation 重验、Map 容器防原型污染、object-ID binding 验证（存在/类型/room）
- [x] 16.13 canonicalization 反射异常边界：全部反射操作 try/catch、revoked/throwing Proxy 结构化拒绝、getter 零调用保持、canonicalization_fault 明确拒绝（callback/授权/registry 零变化）
- [x] 16.14 确定性测试全覆盖：durable authority cohesion、outcome 单调性、opaque bundle 防伪、atomic redemption 注入、digest 绑定、resolution closure、policy、write readiness、structure binding、Proxy、operation-count fixture
- [x] 16.15 验证与证据：五命令验证、真实 writer 文件零改动确认、evidence 文档（round10-durable-authority-bundle-atomicity-local-validation.md）、预算锚点更新

## 17. 第十一轮 Immutable Registries & Durable Cohort Closure

- [x] 17.1 pre-execution authorization fault durable authority：internal_authorization_fault 的 not_started/rolled-back/callback-未调用事实语义；Memory.runtime.treasury.authorizationFaults（version 1、key "af:"、上限 64、load 全量验证）；redemption 回滚后写 marker 前建立 authority；恢复协议（acknowledgeRolledBack、完整 identity 验证、not-executed preExecution tombstone、清 marker+删 entry、幂等、global reset 可完成、其他 fault 不可用该通道、无无条件 clear）
- [x] 17.2 immutable adapter registry：注册快照固定函数引用+冻结 record、registration identity（kind+version+generation+implementation ID）、同 kind/version 不同实现拒绝、更高 version 替换、原对象修改不影响 registry、execution/reconciliation 同一 record、seal（生产装配后动态注册拒绝）、registry revision 进 readiness/contract/cohort、adapter 函数异常边界（validate/derive/structureBindings/durableFacts 拒绝、execute 走 execution unknown、reconcile 拒绝且隔离）、读 API 不泄漏内部 record
- [x] 17.3 immutable policy registry + Treasury-computed digest：注册快照冻结、policyVersion 正安全整数强校验、同 ID/version 不同实现拒绝、可 seal；decision 接口删自报 digest（只返回 withhold/strategicReserve/emergencyOverride/auditReason）、Treasury 按 canonical context（contract/actionKind/resource/rooms/owner/tick/registration identity）自行计算 digest、evaluate 抛错 policy_fault fail closed、emergency override 进 cohort 与审计、redemption 验证 exact registration identity（前缀比较删除）
- [x] 17.4 durable authorization cohort：cohort facts（owner/policy ID+version+registration+decision digest/withhold+reserve+override/epoch/五元 revisions/adapter registration/contract ID+digest/transactionId/leg 摘要≤8/receiver capacity 摘要/issuedTick/bundle identity）+ authorizationCohortDigest；写入 intent/quarantine；unresolved authority/capability/resolution prevalidation 绑定比较；不持久化 heap token
- [x] 17.5 统一 durable action identity：durableAuthorityIdentityDigest（transaction/digest/contract/adapter registration/actionKind/postings/完整 structure descriptor/durable payload/cohort digest/owner/policy/source；outcome/settlement 不进）；intent 首写幂等/read-back/转移/quarantine 幂等/双权威/capability/resolution/reset recovery 全部同一 digest 比较；同 ID 不同 identity → identity_conflict（store 不动、fail closed、callback 零调用）
- [x] 17.6 outcome/settlement/phase 语义矩阵 + cross-store finalized proof：单一矩阵权威（outcome→合法 settlement 集、quarantine phase→outcome 映射）；progress 组合校验、load 验证、非法组合 store unhealthy/readiness=false/resolution 拒绝；finalized+returned_ok 须 settled receipt 或 committed tombstone、finalized+non_ok/not_started 须 not-executed/rolled-back tombstone；proof 缺失 semantic fault（不自动删除、不直接释放）
- [x] 17.7 legacy quarantine 隔离：legacyV1 authority 拒绝当前 adapter reconciler（legacy_authority_isolated）、不能签发普通 capability、显式只读诊断（entry 不动）、新 adapter version 不解释 legacy、不伪造 contract/cohort identity
- [x] 17.8 resolution authority 封闭：公共 TreasuryService 删 consumeReconciliationCapability/treasuryServiceGeneration/treasuryResolutionGuard；resolutionKernelChannel（unique symbol、non-enumerable、closure 直接调用、WeakSet 注册机制删除、registerTreasuryResolutionKernelForService 移除）；testHarness 重挂 kernel；架构测试白名单（仅 facade/faultResolution/testHarness）；staged 写入后才消费语义保留
- [x] 17.9 完整 structure descriptor（AC4）：bindingKind/role/room/locationKind/objectId/expectedType/expectedRoom/incarnation/required/version 全字段进 contract digest（AC4）与 intent/quarantine/durable identity；posting 腿 role 自动派生（source/target）、同结构不同 role 不合并、label 仅诊断、required 缺失构建拒绝、reconciler 收完整 descriptor、v3→v4/v2→v3 迁移、descriptor 有界
- [x] 17.10 facade 职责拆分（行为保持）：authorizationLedger（registry/budget/bundle 签发/atomic redemption/policy-cohort）、resolutionAuthority（capability registry/validate/consume/kernel/路由）、recoveryCoordinator（转移/semantic matrix/cross-store proof/pre-execution recovery/beginTick 恢复）、readinessCollector（状态收集+单一评估器）；facade 只留生命周期编排/read-query facade/模块组装；不直接持有 bundle Maps/capability WeakSets/kernel 细节；无新循环依赖；公开行为兼容
- [x] 17.11 清理与 evidence 修正：删除 fix-ac3.cjs 与 fix-resolution.cjs；Round 10 evidence 修正（不再声称已移除、如实记录第十一轮删除、不篡改历史）；git tree 无写死本地绝对路径的临时修补脚本
- [x] 17.12 确定性测试：internal fault recovery（回滚/callback 零调用/durable authority/global reset/专用 resolution 幂等/其他 fault 拒用/不再永久锁）、adapter registry（原地修改不影响/同 version 拒绝/更高 version 旧 contract 失效/seal/异常边界/同 record/读不泄漏）、policy registry（原地修改不影响/同 ID+version 拒绝/version 非法/抛错 fail closed/自报 digest 不可信/非法字段拒绝/revision 变化 bundle 失效/override 进 cohort）、cohort（owner/policy/decision/leg/capacity 变化 digest 变化、global reset 可读、capability/resolution 严格绑定）、full identity（同 ID 异 owner/policy/descriptor/payload → conflict、读回不一致保留 intent、双权威 cohort 不一致 fail closed、完整 identity 幂等、capability 绑错拒绝）、semantic matrix（非法组合 unhealthy、finalized proof 有无两态、readiness=false）、legacy（拒绝当前 reconciler/普通 capability/诊断不动/新 version 不解释/不伪造）、resolution privacy（公共面不存在 consume/generation/guard、普通模块不能 import kernel、普通对象不能提前消费、service 正常 resolution、harness 外不可达）、descriptor（role/expectedType/expectedRoom/objectId 变化 digest 变化、同结构异 role 不合并、reset 后仍在、reconciler 收到、required 缺失拒绝）、facade decomposition（公共 API 行为保持、ledger/capability 不在 facade、recovery 语义、架构测试阻止绕过）、cleanup（fix-ac3 不存在、evidence 一致、无本地路径脚本）
- [x] 17.13 operation-count fixture：query/authorize/prepare 正常路径无新增 intent/quarantine/reservation 全扫；registry lookup/revision O(1)；cohort/identity 计算与 leg/posting 数线性
- [x] 17.14 验证与证据：五命令验证、真实 writer 文件零改动确认、evidence（round11-immutable-registries-durable-cohort-local-validation.md）、预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的前置 commit）；未部署/未合并/未接真实 writer

## 18. 第十二轮 Durable Integrity Proof & Stable Reconciler Identity

- [x] 18.1 authorization fault staged publication：authority 写入结果不忽略（written/already_present 且完整 identity 一致才发布普通 internal_authorization_fault marker；capacity_exhausted/store_fatal/identity_conflict/invalid_entry → 显式 internal_authorization_fault_forensic marker fail closed，仅显式 forensic acknowledge 通道解除）；写入前 identity 重算 + 写入后 read-back 完整身份验证（不一致回退写入、不发布 marker）；write readiness 增加 authorizationFaultCapacityExhausted 前置容量 admission（prepare 授权前阻断，不再等故障后发现 store 已满）
- [x] 18.2 authorization fault entry v2 完整 durable identity：完整 cohort facts、adapter version/registration/semantic identity、owner/policy identity、structure facts、postings、durableIdentityDigest（由持久事实可重算）；同 ID 幂等仅限完整 identity 一致、不同 → identity_conflict 原数据不动；posting.resource ∈ RESOURCES_ALL、locationKind 受控枚举、key/entryCount/安全整数/上限/未知版本 fail closed；v1 无损迁移标记 legacyV1（身份不完整隔离，不猜测）
- [x] 18.3 resolution tombstone v3 绑定 attempt identity：contractDigest/authorizationCohortDigest/durableIdentityDigest 可选字段（缺省 = legacy proof，不得证明携带现代身份的 attempt）；faultResolution 全部 already_resolved 快路径按 capability/attempt identity 完整比较（conflict/insufficient 均 fail closed）；resolve-as-committed/not-executed 写入绑定 authority identity；recoverStagedResolutions 的 final not-executed 补释放与 resolving finalize 释放前验证 identity（旧 tombstone 不释放同 ID 新 attempt）；pre-execution 解除后重复 resolution 幂等须携带匹配 digest；v2→v3 无损迁移
- [x] 18.4 receipt v4 settlement proof：settled value 升级为 { settledAtTick, digest?, durableIdentityDigest? }（v1/v2/v3 纯数字迁移为无身份 legacy proof）；commitSettledReceipt/refreshSettledReceiptForResolution 绑定 attempt 身份（staged commit 路径携带 durableIdentityDigest）；checkTreasuryFinalizedProof 接收完整 attempt identity——returned_ok 只能由 identity 匹配的 committed proof 证明、其余只由 identity 匹配的 not-executed tombstone 证明；proof 不足/不一致保留 intent fail closed
- [x] 18.5 跨 global reset 稳定 adapter/reconciler 语义身份：adapter 注册显式声明 semanticIdentity（1..128 字符、随代码版本化、注册时冻结）；registry 同 kind/version 不同语义身份拒绝（语义演进必须升 version）；AC4 digest/cohort facts/durable identity/intent/quarantine/authorization-fault/capability 签发全链路绑定；global reset 后同 kind/version 但语义身份不同 → capability 签发拒绝（不得调用当前 reconciler 解释旧 authority）；authority 缺失语义身份（含旧数据/低层路径）→ 隔离不猜测；低层（非 contract）路径在 intent 写入时从当前 registry 绑定语义身份（contract 路径以 contract 携带值为准，durable identity 输入同源）；per-global registrationId 防线保留
- [x] 18.6 统一 canonical digest 重算（identityProof.ts 单一 helper）：cohort digest 由持久化 canonical cohort facts 重算（不重执行 policy resolver、不信任自带 digest）；durable identity 由 entry 持久不可变事实重算（不信任自带 durableIdentityDigest）；覆盖 intent/quarantine/authorization-fault 的 load 全量验证、写入前重算、capability 签发（unresolved authority 事实校验）、resolution prevalidation、intent→quarantine 转移（两侧不一致保留原 authority）、显式 repair（不覆盖 digest，重算失败拒绝）；篡改事实保留旧 digest → store unhealthy / authority inconsistent fail closed
- [x] 18.7 structure binding discriminated union：bindingKind 在 validateStructureBindings 唯一 canonicalization 点推导并显式化（posting binding 恒 governed_location）；governed_location 携带 objectId 或 expectedType/expectedRoom → 拒绝；显式 game_object 缺 objectId → 拒绝；identity key/构建验证/执行前重验/descriptor 派生全部按已确定 discriminant 分支（不再以 objectId !== undefined 判定）；descriptor 全字段继续进 AC4 digest 与 durable identity
- [x] 18.8 forensic incomplete authority 隔离：recovery coordinator 在 intent 缺失时防御性直写的最小 quarantine 显式标记 forensic { reason: intent_missing_fallback, detail }（与 legacyV1 故障来源可区分）；继续阻断 writer 并保留 postings；不签发普通 reconciliation capability（legacy_authority_isolated）；普通 resolve 入口拒绝；只读诊断 treasuryForensicQuarantineDiagnostics（来源/缺失证明/隔离原因）；仅显式人工修复/迁移或专门 forensic resolution 处理
- [x] 18.9 production 类型边界：TreasuryActionExecutionRequest.authorization 收紧为 opaque TreasuryAuthorizationBundle 单一类型（裸 token/token 数组仅 test 显式 cast 边界，kernelChannel test-only 低层通道保留）；无 intent 两阶段 prepare/commit 的 commit fault quarantine 按 3.8 进入 forensic 隔离（fail closed）
- [x] 18.10 确定性测试：treasuryRound12Integrity.test.ts（31 用例，覆盖 fault publication 六类、tombstone/settlement proof identity、stable identity 四态、digest 重算五类篡改、structure union 拒绝集、forensic 隔离五断言、v1→v2 迁移/未知版本 fail closed、正常 commit/abort 与 query 零写回归）；既有 fixture 按新不变量适配（semanticIdentity 显式声明、receipt v4 proof、repeated-resolve 携带 digest）；Memory schema 指纹锚点更新
- [x] 18.11 验证与证据：typecheck（build+test）/treasury 定向/全量 Jest（224 suites / 1299 tests）/build/verify-jest-budget PASSED；evidence（round12-durable-integrity-proof-local-validation.md）；预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的 7ebd7d7）；未部署/未合并/未接真实 writer

## 19. 第十三轮 Receipt Migration Safety & Modern Proof Strictness

- [x] 19.1 统一 normalized receipt lookup：单一 lookup 结果（absent / legacy_committed / modern_committed / corrupted / incompatible）供 hasSettledReceipt、readTreasurySettlementProof、admitTreasuryReceipt、reserveTreasuryReceiptAdmission、commitSettledReceipt、refreshSettledReceiptForResolution、cleanup/migration、projection.isSettled、prepareTransaction、finalized proof 全部路径复用；不得一处 typeof === "number" 另一处只认对象
- [x] 19.2 query 零写识别 legacy：v1/v2/v3 未迁移 store 中可可靠识别的合法数字 value 视为已结算（hasSettledReceipt 不再把合法数字当 corrupted）；只读查询零 Memory 写入、不隐式迁移；v1 裸键 O(1) 双键探测；unknown version 与损坏 value 仍 fail closed
- [x] 19.3 receipt v5 显式 proof level：TreasurySettlementProof 增加 level: "modern" | "legacy"（modern 必填 digest + durableIdentityDigest；legacy 禁携带身份字段）；v3/v4 → v5 一次性版本化迁移（数字 → 显式 legacy committed proof；完整身份 → modern；部分身份 fail closed 原store保留）；迁移临时结构全量校验后原子替换、幂等、碰撞安全；admission 修复 already-settled 命中（migrated v4/v5 对象 proof 不再漏判）
- [x] 19.4 already_settled 零发布：compat 单阶段（writeAcceptedTransaction）收到 already_settled 任意形态 → 零 journal/overlay/capacityDeltas/heap settled cache/onRecorded/projectionRevision/tentative 写入并返回 already_settled；staged commit 的 receipt 发布段 fatal/identity-conflict/insufficient → fault（不发布 heap）；post-callback 防御段读取完整 proof 按 identity 区分（match 幂等不重复发布；legacy/insufficient 不发布 heap、保留 intent/quarantine；conflict 明确 internal settlement fault）
- [x] 19.5 receipt commit 结果细化：written / already_settled_match / already_settled_insufficient(legacy) / identity_conflict / fatal 五态（不再把所有 existing proof 折叠为无法判断身份的普通 already_settled）
- [x] 19.6 identity-aware refresh：refreshSettledReceiptForResolution 按 absent→written(modern proof)、modern+match→刷新 settledAtTick、modern+conflict→拒绝（保持 resolving authority）、legacy/insufficient→拒绝覆盖（保持隔离）、corrupted→fatal；resolutionStore/faultResolution 调用方携带完整 identity 并处理 blocked 结果
- [x] 19.7 staged recovery 只接受 match：recoverStagedResolutions 全部分支（resolving committed、final not-executed 补释放、global reset 补完成）以 relation === "match" 为唯一释放许可；conflict 与 insufficient 分别保留 authority、独立诊断与计数（不再 !== "conflict"）
- [x] 19.8 显式 proof/authority 等级：intent v5 / quarantine v4 / authorization-fault v3 持久 authorityLevel: "modern" | "legacy" | "forensic" | "lowlevel"；版本化迁移按字段完整性一次性定级（modern 矩阵全齐→modern；forensic/legacyV1 标志→forensic/legacy；仅 durable identity→lowlevel；无现代事实→legacy）；load 校验 modern required 字段矩阵（缺失 → store unhealthy，绝不降级 legacy）；cohort facts 与 digest 成对存在（一方缺失 → unhealthy，不再自动当 legacy）；authorityLevel 缺失（新版本 store）→ unhealthy；capability 签发拒绝 legacy/forensic
- [x] 19.9 集中 cohort validator：cohortValidation.ts 唯一 validateTreasuryAuthorizationCohortFacts（全字段 + 数组/字符串上限 + 安全整数 + nested object + transaction/entry 交叉一致性）；intent/quarantine/authorization-fault 三 store shape 校验共用；canonical 重算（recomputeTreasuryCohortDigest / verifyTreasuryEntryIdentity）异常边界（throwing Proxy/缺字段/null → 结构化错误，永不抛出中断 tick）；cohortValidationFailures 计数
- [x] 19.10 集中 structure descriptor validator：structureDescriptorValidation.ts 唯一共享 validator（governed_location 禁 objectId/expectedType/expectedRoom；game_object objectId 必填 + expectedType/expectedRoom 规则 + structureId/objectId 语义一致）；contract canonicalization / intent / quarantine / authorization-fault / durable identity 重算 / capability 签发共用；单一 bindingKind 分支权威；持久矛盾 descriptor → store unhealthy
- [x] 19.11 forensic attempt identity：write-fault marker 扩展 attemptIdentity（contractDigest/authorizationCohortDigest/durableIdentityDigest——redemption 故障前已计算）；forensic resolution tombstone 绑定同一 identity；already_resolved 比较完整 identity（treasuryAttemptIdentityRelation）；旧 marker（无 identity）按 legacy forensic proof 处理不得证明 modern attempt；acknowledgeRolledBack 必填保留；无新增无条件 clear 接口
- [x] 19.12 确定性测试：receipt migration 全场景（v3 数字 query 零写/contract 零 callback/prepare already_settled/compat 零 heap/迁移幂等/碰撞损坏 fail closed/legacy 显式标记/unknown version 不遗忘/retention 清理）、existing proof commit 行为（match 幂等/conflict fault/insufficient 隔离/compat 零变化）、refresh/recovery（legacy 不覆盖/conflict 不 finalize/match 可 finalize/insufficient 不释放/独立计数）、proof levels（删除必填字段 unhealthy 不降级/legacy 迁移显式标记/forensic 显式/双 authority 不一致）、cohort validation（缺 ownerIdentity/null revisions/错误类型/throwing Proxy/篡改 digest unhealthy/repair 不覆盖）、structure descriptor（governed+objectId unhealthy/governed+expectedType unhealthy/game_object 缺 objectId/语义不一致/同一 validator/同结构异 role）、forensic identity（同 ID 不同 cohort 不共享 tombstone/marker 完整 identity/legacy marker 不证明 modern/acknowledge 必填/完整 identity 幂等）、回归（正常 contract→bundle→intent→OK→commit、non-OK→abort、unknown→quarantine、committed/not-executed resolution、query 零写、生产 writer 零接入）
- [x] 19.13 operation-count fixture：legacy lookup / receipt migration / identity match / conflict / insufficient / proof-level rejection / cohort validation failure 计数区分；query 路径 O(1) 无迁移触发；正常 admission O(1)；staged recovery 不因单条 insufficient 全表扫描
- [x] 19.14 验证与证据：typecheck / build / treasury 定向 Jest / 全量 Jest / verify-jest-budget 全部真实通过；evidence（round13-receipt-migration-modern-proof-local-validation.md，记录 receipt 版本迁移表、normalized lookup 状态、proof 等级矩阵、modern required fields、staged recovery 语义、forensic identity、新增 Memory 字段与版本、operation-count、真实命令输出与 suite/test 数字、未部署/未合并/未接真实 writer）；预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的前置 commit）

## 20. 第十四轮 Resolution Proof Closure & Authority-Level Integrity

- [x] 20.1 staged committed 三方 proof 闭环：recoverStagedResolutions 的 resolving+committed 分支重构为统一 verifier——无论 receipt tick 是否充分都读取完整 receipt proof 并按 tombstone 完整 attempt identity 验证 relation；receipt ↔ tombstone、tombstone ↔ authority、receipt ↔ authority 三方 match 才释放（释放 quarantine + intent、清匹配 marker、finalize）；authority 已不存在（前一 global 已释放、finalize 前中断）仍要求 receipt ↔ tombstone match 且 tick 足够才补完成；receipt 不存在/tick 不足 → identity-aware refresh → 成功后重新读取持久 proof 再完整验证（不凭 refresh 返回值释放）；hasSettledReceipt 的 tick 查询只用于 replay blocker，不作 authority release 证明；conflict / insufficient 独立计数与诊断
- [x] 20.2 full receipt identity propagation：recoveryCoordinator.checkTreasuryFinalizedProof 与 faultResolution not-found 幂等路径的 receipt proof 视图完整传递 digest/contractDigest/authorizationCohortDigest/durableIdentityDigest（不再只传 digest+durableIdentityDigest 子集——cohort/contract 不同即 conflict、缺失即 insufficient）
- [x] 20.3 双 authority 等级矩阵：resolveTreasuryUnresolvedAuthority 双存在分支先分别从持久事实重算 identity（任一失败整体 inconsistent——durable 字符串相同也不信任）；显式比较 authorityLevel（authorityCompatibility.ts 单一权威矩阵：同等级 comparable、任何跨等级组合 inconsistent）；modern+modern 要求 durableIdentityDigest/authorizationCohortDigest 双方完整存在且相等 + contractId/contractDigest/adapterSemanticIdentity 一致（一方缺失即 inconsistent，不退回 optional 子集）；lowlevel+lowlevel 严格 durable 比较；不一致时 capability 零签发、resolution 零副作用、两份 authority 保留
- [x] 20.4 lowlevel 严格矩阵：authorityLevel.ts 定义 lowlevel required（digest/kind/actionKind/source/postings 非空 + durableIdentityDigest + 显式 lowlevelSource 来源标记 runtime-lowlevel@v1）与 forbidden 现代字段（contractId/contractDigest/authorizationCohort(+Digest)/authorizationDigest/adapterRegistrationId/ownerIdentity/policyIdentity）；legacy 禁止任何现代身份字段；forensic 无矩阵（隔离容器）；低层写入缺省 durableIdentityDigest 时由事实确定性派生（与 facade 生产路径同源）
- [x] 20.5 production 定级边界：facade.executePreparedAction 的 intentContract 与 redeemedCohort 必须成对（都有 → modern、都无 → lowlevel、只有其一 = partial-modern → authority_invariant_violation 结构化拒绝 + callback 零调用 + 预留释放）；authorizationLedger 的 bundle-redemption fault 在 cohort 缺失时定级 forensic（不再 lowlevel）；contract 路径绝不写 lowlevel
- [x] 20.6 旧 entry migration 定级重构：classifyTreasuryAuthorityLevelForMigration 取代三段式——partial-modern 一律 forensic 隔离（绝不自动 lowlevel）；显式等级的上一版 entry（intent v5 / quarantine v4 / fault v3）按 priorLevel 复验（显式 lowlevel 满足严格矩阵 → lowlevel + migrated-lowlevel@v1 来源标记；显式 modern 矩阵缺失 → forensic；显式 legacy 携带现代字段 → forensic；重算矛盾 → fatal）；cohort XOR / digest 与事实重算矛盾 → fatal 原数据保留；迁移先构造临时 entries、全量验证后原子替换（任一 entry 定级失败原 store 不变）、幂等
- [x] 20.7 resolution tombstone 显式 proof level：resolutions v4 携带 proofLevel: identity-bound / lowlevel / legacy / forensic（required/forbidden 字段矩阵由 proofLevel 声明，不再由 optional 字段存在性猜测）；同 id 覆盖只允许保持同一 proof level 与完整 attempt identity；v3 迁移（全身份 → identity-bound、全缺 → legacy、部分 → forensic）；proof class ↔ authority 等级释放权限矩阵（identity-bound 只释放 modern、lowlevel 只释放 lowlevel、legacy/forensic 不释放 modern/lowlevel authority——错配 insufficient 阻断）
- [x] 20.8 写入前与 read-back 统一重算（durablePublication.ts 共享协议）：intent/quarantine/authorization-fault 写入前从候选事实重算 cohort/durable identity（不一致 invalid_entry 拒绝、entryCount/revision 不变）；发布后从 Memory 持久副本再次重算 + 23 项完整身份字段比较（含 authorityLevel/lowlevelSource/cohort/descriptors/postings/outcome/settlement/source 深度比较）——不一致回滚本次写入并恢复 entryCount/revision/updatedAt，返回 store fatal；同 id 既有 entry identity 不可重算 → 不返回 already_present；调用方输入对象修改不影响已写 Memory 副本
- [x] 20.9 intent → quarantine 转移 read-back 完整化：read-back 比较 authorityLevel、durable identity（重算）、cohort identity、contract identity、adapter semantic identity、durable payload/version、structure descriptors、canonical postings、execution outcome、settlement、source（compareTreasuryAuthorityPublicationReadBack）；lowlevelSource 随事实原子转移；任一不一致源 intent 保留为 emergency authority
- [x] 20.10 authorization-fault health 门禁：轻量 probe 检查 store version 受支持集合 / entries 普通对象 / entryCount 非负安全整数且 ≤ 硬容量 / updatedAt 合法安全整数（明显矛盾直接 unhealthy，不扫全表）；write readiness 的 authorizationFaultUnresolved source 升级为完整 validation 门禁（ensureTreasuryAuthorizationFaultStoreValidated——首次有界全表扫描、heap 缓存后 O(1)、store 不存在零写）；authorizationSafe 联合判定增加 authorization_fault_unhealthy 维度（损坏 → 授权上下文不安全 → callback 零调用）
- [x] 20.11 Memory store 版本与 schema：intents v6 / quarantine v5 / authorizationFaults v4 / resolutions v4（持久语义变化正确提升版本，不在相同 version 下改变解释）；entry 新增 lowlevelSource（低层来源标记）与 proofLevel（tombstone proof class）；schema 指纹锚点更新
- [x] 20.12 确定性测试：treasuryRound14ResolutionProof.test.ts（56 用例，覆盖三方 proof 闭环全场景 / full identity propagation / 双 authority 等级矩阵 / lowlevel required-forbidden / production 不写 lowlevel / partial-modern migration 隔离 / tombstone proof level 矩阵与迁移 / immediate recomputation 注入回滚 / 转移 read-back / fault health 门禁）+ treasuryRound14OperationCount.test.ts（receipt 单条 proof 读取不全扫 / 双 authority 比较不全扫 / write readiness 缓存快路径）；既有 suites fixture 按新矩阵适配（真实派生 durable identity、显式 proofLevel、版本号与指纹、迁移断言 partial-modern → forensic、contract 注入补 cohort）
- [x] 20.13 验证与证据：typecheck（build+test）/ build / treasury 定向 Jest（34 suites / 701 tests）/ 全量 Jest（228 suites / 1407 tests）/ verify-jest-budget 真实通过；evidence（round14-resolution-proof-authority-level-local-validation.md）；预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的 e740fb2）；未部署 / 未合并 main / 未接真实 writer

## 21. 第十五轮 Resolution State Monotonicity & Cross-Store Authority Release

- [x] 21.1 自动恢复统一 unresolved authority resolver：recoverStagedResolutions 的 resolving committed 分支与 final not-executed 补完成分支全部改经 resolveTreasuryUnresolvedAuthority（`quarantine ?? intent` 旁路删除）；ok → 使用 normalized authority 继续 proof 验证；inconsistent → intent/quarantine/tombstone/marker 全保留、独立 authorityInconsistent 计数、write readiness 保持阻断、零 release / 零 receipt refresh / 零 stage 变化；not_found → committed 仍须 receipt ↔ tombstone match 补完成、not-executed 视为释放已完成、不伪造 authority；recovery 不得自行实现 quarantine 优先 / intent fallback / 等级比较 / durable identity 比较
- [x] 21.2 resolution tombstone 不可逆状态机（resolutionStateMachine.ts 单一权威 + resolutions v5）：合法创建 = absent→resolving committed、absent→final not-executed；合法更新 = resolving committed→final committed（transactionId/digest/proofLevel/完整 attempt identity/resolution kind/actionTick/observationTick/reconcilerKind/source/settledAtTick/forensic provenance 全保持，仅 stage 与 resolvedAtTick 单调推进）；禁止 resolving committed→final not-executed、resolving not-executed、final→resolving、final committed→not-executed、final not-executed→committed、同 ID 改 digest/actionTick/proofLevel/attempt identity/降低 settledAtTick；exact idempotence（final/resolving 同完整内容幂等，非覆盖写）；状态 conflict 时原 tombstone 完全不变；write 入口与 recovery finalize 均经状态机
- [x] 21.3 resolving 期间 reconciler/capability 互斥（facade capability gate）：签发前读 resolution tombstone——resolving → 不调用 adapter.reconcile、不签发、resolution_in_progress；final 且 identity match → already_resolved 语义、reconciler 零调用；tombstone identity conflict / proof insufficient → fail closed 零调用；reconciler 调用计数测试证明 resolving 期间为 0；恢复完成后才允许新 transaction 生命周期
- [x] 21.4 forensic provenance（forensicProvenance.ts）：显式 provenance 语义（协议/acknowledgement/管理身份或 capability digest/attempt identity/确认时间/来源/自动补完成许可）+ 形状校验；migration-derived forensic tombstone 永久隔离（普通 beginTick 不释放、不参与 capability resolution、不假装已确认）；普通自动 recovery 释放矩阵收敛为 identity-bound→modern、lowlevel→lowlevel（legacy/forensic 不自动释放，legacy replay blocker 保留）；forensic 诊断保留来源
- [x] 21.5 按 authority class 的 same-ID 幂等（authorityIdempotence.ts 唯一权威）：modern（等级+durable 双方完整一致+cohort+contract）；lowlevel（等级+受控 source+durable+digest/kind/postings）；legacy（受控 legacy signature：transactionId/digest/kind/actionKind/canonical postings/source/legacyV1）；forensic（reason/provenance+已知 attempt facts+digest+postings+source+phase/outcome）；跨等级永远冲突；provenance 不足 → 不视作 same；intent/quarantine/authorization-fault 三 store 写入统一接入；不再依赖空 durable digest 匹配
- [x] 21.6 store-specific durable publication 语义验证：publication helper 支持注入 store-specific 完整 shape+语义 validator（intent level/outcome-settlement 矩阵、quarantine phase/outcome/settlement+forensic+legacyV1+tick/recordedAt+deltas+contract 事实、authorizationFault outcome=not_started/rollbackConfirmed=true/faultTick/source/detail/authority 矩阵）；比较字段补齐 phase/forensic/legacyV1/faultTick/rollbackConfirmed/tick/recordedAt/createdAtTick/detail；必须检出"phase 被篡改但 digest 未变"；回滚恢复 entry/entryCount/revision/updatedAt（authorizationFaults 回滚不再错写 updatedAt=Game.time）
- [x] 21.7 authority 读取完整深冻结（durableSnapshot.ts bounded helper）：quarantine read/list 补 authorizationCohort 与 forensic 深冻结；authorizationFault read 补 structureFacts/authorizationCohort；resolution tombstone read 补嵌套 forensic provenance；调用者修改任意嵌套字段 Memory 不变；helper 只处理有界普通对象/数组
- [x] 21.8 lowlevel provenance 受控权威：validateTreasuryLowlevelSourceField 收敛为严格枚举（runtime-lowlevel@v1 / migrated-lowlevel@v1；test-only 仅测试通道）；未知 source 拒绝；migration 只生成 migrated 来源、无法证明来源的旧 lowlevel 定级 forensic；runtime 来源只由 store 内部缺省写入；source 进入 lowlevel same-ID 比较（source 不同 → conflict）
- [x] 21.9 immediate 与 staged recovery 共用三方 verifier（committedProofVerifier.ts 纯函数）：输出 verified/conflict/insufficient/authority_inconsistent/receipt_absent/receipt_stale；normal resolve-as-committed、beginTick staged recovery、补完成与 already-resolved 检查复用；immediate 流程改为 写 resolving → refresh → 重读持久 proof → verifier → 通过才释放 → finalize；refresh 成功但 receipt 被篡改 / 双 authority 变 inconsistent / proof 变 legacy 均 not release；verifier conflict/insufficient 计数与 recovery 一致
- [x] 21.10 resolution health 版本兼容：轻量 probe 受支持版本集合 = loader 集合（v1/v2/v3/v4/v5，supported migration pending 而非 unknown）；未 load 不全表扫描（resolving 计数 heap 缓存，写路径触发完整 load/migration）；readiness resolving blocker 改经缓存计数；unknown v99 仍 fail closed
- [x] 21.11 确定性测试：cross-store authority recovery（inconsistent 零释放/双保留/tombstone 保持 resolving/marker 保留/modern 双不一致同样零释放/一致可继续/旁路删除/独立计数）、tombstone 状态机全迁移矩阵与 exact idempotence、resolving capability gate（reconciler 零调用/identity conflict/already-resolved）、forensic provenance（migration-derived 不释放/无 provenance 隔离/legacy-forensic 不释放 modern-lowlevel/诊断保留）、class-specific idempotence（legacy 不同 digest 空 durable → conflict、同 digest 不同 postings → conflict、完整 signature → 幂等；forensic reason/attempt facts；modern 等级；lowlevel source）、publication semantics（phase/forensic reason/legacyV1/faultTick/rollbackConfirmed/outcome-settlement 篡改回滚、store validator 调用、bookkeeping 恢复、callback 零调用）、deep snapshot（cohort.revisions/forensic/descriptor/cohort legs/tombstone/list 嵌套封闭）、lowlevel provenance（未知 source 拒绝/runtime 内部通过/migrated 仅迁移/迁移 forensic/source 变化 conflict）、immediate committed verifier（三方 match 释放/receipt 篡改/inconsistent/legacy 不释放/同 verifier 复用/计数一致）、resolution health（v3 不误报 unknown/写路径触发迁移/v99 fail closed/轻量不全扫）、回归（Round 13/14 全场景、正常 committed/not-executed resolution、production writer 边界扫描、真实 writer 文件 diff 为空、query/readiness 零写）
- [x] 21.12 operation-count fixture：inconsistent 双 authority 恢复不扫全 store；resolving capability gate O(1)（单条 tombstone 读取）；store-specific read-back 不额外扫描历史 entry
- [x] 21.13 验证与证据：typecheck / build / treasury 定向 Jest / 全量 Jest / verify-jest-budget 真实通过；evidence（round15-resolution-state-monotonicity-cross-store-authority-local-validation.md，含状态迁移图、cross-store authority 状态图、forensic 自动/显式路径区别、authority class 幂等矩阵、publication read-back 验证矩阵、lowlevel provenance、committed verifier 复用路径、store 版本/容量/迁移、operation-count、真实命令与 suite/test 数字、未部署/未合并/未接真实 writer/GitHub 无 CI 本地验证、Screeps hard CPU interruption 与 Memory flush 边界不保证 exactly-once）；预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的前置 commit）

## 22. 第十六轮 Attempt Rearming & Execution-Fact Cohesion

- [x] 22.1 显式 attempt rearm 协议（attemptRearm.ts）：deriveTreasuryRearmChildTransactionId（canonical tuple 绑定协议版本 + parent ID + parent attempt identity 含 lowlevelSource → `tr1_<hash16>`，满足现有 validator、长度有界、无随机数、跨 global reset 恒定、同 parent 幂等、不同 parent identity 不同 child）；rearmResolvedNotExecutedAttempt 受控入口（零写）：parent final not-executed tombstone 存在、proofLevel 为 identity-bound/lowlevel、expectedParentIdentity 可选强校验、resolver not_found（authority 已释放）、marker 清理完成（7.3）、各 store 健康；facade 暴露 service.rearmResolvedNotExecutedAttempt
- [x] 22.2 same-ID 不可重试：TreasuryFaultResolutionResult 的 reprepareAllowed 删除，改为 sameIdRetryAllowed: false + rearmChildTransactionId（not-executed 时确定性派生）；prepareTransaction 与单阶段 compat 入口在 final not-executed tombstone 存在时拒绝（reason=rearm_required，callback 零调用）；pre-execution/forensic 通道同样不再返回可重试语义（forensic 不返回 child ID——proof 不足不可 rearm）
- [x] 22.3 execution-fact cohesion（executionFactCohesion.ts 唯一权威）：outcome 对等矩阵（returned_ok 单侧、aborted_final 并存、任意不同 outcome → inconsistent）；phase 类别严格对应（跨类上探并存禁止）；intent settlement 并存矩阵（ready/finalized 不得与 unresolved quarantine 并存）；归一化合并规则（outcome=共同值、settlement=更进展一方、phase=quarantine 权威）；unresolvedAuthority 双 authority 分支接入——cohesion 不一致 → inconsistent（两份全保留）
- [x] 22.4 resolver 四态 store_unhealthy（unresolvedAuthority.ts）：store health 前置（已存在 store 触发 load validation；store 不存在 = 合法无 entry）；任一 fatal → store_unhealthy 附 quarantine/intent store 错误；readEntry undefined 不再被折叠为 not_found；capability 签发（authority_store_unhealthy 拒绝 + reconciler 零调用）、prevalidate（rejected）、recoverStagedResolutions 两分支（零副作用 + storeUnhealthy 计数）、committedProofVerifier（authority_store_unhealthy verdict）、rearm（拒绝）全部接入
- [x] 22.5 final not-executed 残留 marker 安全补完成（resolutionStore.validatePendingReleaseMarkerCompletion）：not_found 时检查 write-fault marker——不存在=完成移出索引；全部匹配（transaction/digest/attemptIdentity relation=match/phase 兼容矩阵/proof level 兼容）才清除；他属/conflict/insufficient/phase 不兼容 → 保留 + markerCleanupBlocked 独立计数；readTreasuryWriteFault 返回深冻结快照（7.2）；recordTreasuryWriteFault 深拷贝 attemptIdentity
- [x] 22.6 resolution 持久状态语义矩阵（resolutionStateSemantics.ts）：validateTreasuryResolutionTombstoneState(entry, load|write)——stage 显式、resolving 只能 committed + settledAtTick 必填 + proofLevel 只能 identity-bound/lowlevel + 无 forensic provenance/preExecution、final committed 缺 settledAtTick 非法、final not-executed 无 settledAtTick + preExecution 来源一致、forensic provenance 只配 forensic、final 时序校验；接入 load（validateResolutionStoreShape 每条）、写入候选（write 上下文：新 lowlevel 必须携带 lowlevelSource、identity-bound 禁携带）；recovery 防御分支删除路径废除（保留 + 计数）；v2-v4 迁移补终态 stage=final
- [x] 22.7 authority 写入输入别名隔离（durableClone.ts）：cloneTreasuryDurableValue 有界深拷贝（深度 8/键 256，超出退化浅拷贝）；intent/quarantine/authorizationFaults 写入口整体深拷贝（structureFacts/cohort/forensic 一并隔离）；resolutionStore 写入深拷贝（forensicProvenance）；writeFault marker 深拷贝 attemptIdentity；调用方写入成功后修改原输入 Memory 不变
- [x] 22.8 lowlevel provenance 完整 proof 链：TreasuryAttemptIdentity + treasuryAttemptIdentityRelation 纳入 lowlevelSource 维度（缺失=insufficient/不等=conflict/attempt 无而 proof 有=conflict）；capability 绑定 lowlevelSource（prevalidate 强校验：lowlevel authority 双方必须一致携带、非 lowlevel 禁携带）；resolution tombstone v6 lowlevelSource（仅 lowlevel proof 携带、新写入必填、v5 旧数据隔离不释放）；receipt v6 lowlevelSource（modern 可携带/legacy 禁带/commit 与 refresh 随低层 attempt 写入）；verifier 三方比较（tombstone 缺=insufficient、与 authority 不同=conflict）；facade 两阶段低层路径（无 contract）receipt 写入携带 runtime 来源
- [x] 22.9 not-executed capability 消费顺序（faultResolution）：prevalidate → slot 预检 → consume capability → 写 final tombstone → 释放 → 清 marker → markTreasuryPendingReleaseCompleted；consume 失败零持久副作用（不写 tombstone、authority/marker 保留）；tombstone 写失败 authority 保留（capability 已消费、后续 tick 重签发）；成功后 pending-release 索引即时移出
- [x] 22.10 resolution pending 恢复 O(1) 索引：ResolutionStoreRuntime 维护 resolvingIds/pendingReleaseIds Sets（load 一次全表重建；写入/删除/驱逐/补完成同步维护）；recoverStagedResolutions 空闲 O(1) 快路径（idleFastPath 计数）；有待处理项只遍历索引 ID；Memory 权威（索引 ID 失效即清理，不作安全 proof）；resolving 计数改经索引 size（health/readiness 兼容）
- [x] 22.11 确定性测试：attempt rearm（同 ID prepare 拒绝/显式 rearm child ID/幂等/跨 reset/不同 parent/不同 identity/authority 在场拒绝/marker 未清拒绝/resolving 拒绝/legacy-forensic 拒绝/child 独立故障与 resolution/孙代链/旧 reprepareAllowed 删除/child contract 绑定 child ID/parent proof 不解决 child）；execution fact cohesion（returned_ok+started_unknown 等全部不一致矩阵/returned_ok 不丢失/not-executed capability 不签发/相同 outcome 合法组合归一化/phase 矛盾/identity 同 facts 异不合并/签发与 recovery 零副作用）；marker 补完成（matching marker 清除/conflict/insufficient/他属不清/marker 不存在完成/清除后 rearm 才允许/幂等）；store unhealthy resolver（intent fatal/ quarantine fatal/双 fatal/一侧 healthy 一侧 fatal/staged recovery 不 finalize/final recovery 不释放/签发零 reconciler/verifier 不当 absent）；resolution 持久语义矩阵（resolving not-executed/stage 缺失/final committed 缺 settledAtTick/not-executed 携带 committed 字段/provenance 矛盾 → unhealthy 不自动删除/migration 非法组合原 store 不变/recovery 不删除/v1-v5 迁移继续通过）；write alias isolation（quarantine/intent/authfault/resolution/marker 五 store 嵌套输入修改 Memory 不变）；lowlevel provenance proof 链（runtime/migrated 互不释放/provenance 进 durable identity/tombstone 与 receipt 保存/verifier 严格比较/旧 proof 缺来源 insufficient/modern 不释放 lowlevel/capability 绑定校验）；not-executed capability staging（consume 失败无 tombstone/authority 保留/consume 成功 tombstone 失败不形成错误终态/释放前中断 beginTick 补完成/管理调用幂等/rearm 在 pending-release 完成后）；resolution indexes（空闲 beginTick 不扫描/单 pending 只处理 1 个 ID/pending 进入与移出索引/global reset 重建/retention 同步/索引非安全 proof）；回归（Round 15 状态机与 verifier/Round 13 receipt 迁移/normal contract→bundle→intent→OK→commit/non-OK→abort/unknown→quarantine/committed 与 not-executed resolution（同 ID 不可直接重试）/production writer 边界扫描/真实 writer 文件 diff 为空/query 与 readiness 零 Game 写入）
- [x] 22.12 operation-count fixture：空闲 beginTick 不扫描 resolution store（idleFastPath）；单 pending ID 不扫描历史 final tombstone；store_unhealthy resolver 不回退扫描或选择另一 authority；rearm O(1) 不扫描 receipt/tombstone 历史表
- [x] 22.13 验证与证据：typecheck / build / treasury 定向 Jest / 全量 Jest / verify-jest-budget 真实通过；evidence（round16-attempt-rearming-execution-fact-cohesion-local-validation.md，含 rearm 状态图、parent/child 身份规则、execution fact 兼容矩阵、marker 补完成流程、resolver 四态、持久状态矩阵、lowlevel proof provenance、capability 发布顺序、恢复索引结构、store 版本与迁移、operation-count、真实命令与 suite/test 数字、未部署/未合并/未接真实 writer、GitHub 无 CI 本地验证、Screeps hard CPU interruption 与 Memory flush 边界不保证 exactly-once）；预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的前置 commit）

## 23. 第十七轮 Durable Attempt Lineage & Rearm Capability

- [x] 23.1 durable attempt lineage store（attemptLineage.ts）：Memory.runtime.treasury.attemptLineage v1（key `l:<rootAttemptId>`、entryCount、版本化）；record 含 lineageId/root+current attempt ID 与完整 identity/generation/state/resolutionState/nextChildTransactionId/retrySemanticDigest/authorityClass/lowlevelSource/bindingDigest/rearmable/nonRearmReason/retirement 三段（lineagePublished、authorityReleased、markerCleaned）/createdAtTick/updatedAtTick/recordRevision；状态机单调（retiring→rearm_ready→capability_issued→child_intent_pending→child_active→chain_committed；non_rearmable_retired/forensic_isolated）；禁止回退/同代不同 child/重复 child/root 或语义身份或 class 变化/旧 generation 复用；硬容量（满载新 root 拒绝、同 chain 推进不新增 slot、不自动驱逐、fail closed）；root/current/next O(1) 索引（global reset 首次 load 一次全表验证重建、Memory 权威、不一致 → unhealthy）；写入深拷贝→校验→发布→read-back→回滚；读取有界深冻结快照；调用方输入 alias 不污染 Memory；pending 恢复索引（retiring/capability_issued/child_intent_pending）
- [x] 23.2 final not-executed lineage replacement staged 协议（faultResolution）：prevalidate → resolution slot + lineage 容量与占用预检 → consume capability → 写 final tombstone → 写 lineage candidate + read-back → 释放 intent/quarantine → identity-aware 清 marker → 标记三段完成 → rearm-ready；lineage 容量不足零副作用（capability 不消费/tombstone 不写/authority 不释放）；tombstone 已写 lineage 失败 → authority/marker 保留 + beginTick 重试 publication；resolution 结果删除 rearmChildTransactionId 字符串（表达 sameIdRetryAllowed=false、retirement 完整性、rearm 可申请、cleanup pending）；immediate not-executed（preExecution/forensic 通道）同样建立 lineage
- [x] 23.3 opaque rearm capability（rearmCapability.ts + facade.issueTreasuryRearmCapability）：capability 冻结对象绑定 lineageId/lineageRevision/parentTransactionId/parentIdentityDigest/childTransactionId/generation/retrySemanticDigest/actionKind/adapterSemanticIdentity/authorityClass/ownerIdentity/lowlevelSource/bindingDigest/serviceGeneration/tick/nonce；heap-only WeakSet 对象身份验证；单次消费、跨 tick/跨 service/跨 parent/跨 child/lineage revision 变化失效；JSON 复制与普通对象伪造失败；同 tick 重复 issuance 幂等返回同一 capability；未使用 tick 结束/global reset → 新 service 重签发（child ID 一致）；child durable intent 接管后该 generation 不得重签发；rearmResolvedNotExecutedAttempt 公开字符串入口退役
- [x] 23.4 tr1_ 命名空间门禁：transactionId.ts 单一权威 isTreasuryRearmAttemptId；prepare（含 compat recordAcceptedTransaction）、contract authorization、bundle redemption、durable intent publication、receipt commit 全路径检查 tr1_ 必须匹配 capability；initial attempt 使用 tr1_ 拒绝；手工/公开 hash/无 capability/跨 parent/跨 child/已消费/过期/语义不匹配全部拒绝且零副作用；架构扫描禁止 production 模块导入 child derive helper
- [x] 23.5 retry semantic identity（retrySemanticIdentity.ts 单一权威）：modern digest 绑定 actionKind/adapterVersion/adapterRegistrationId/adapterSemanticIdentity/canonicalArgsText/canonical postings/structure descriptors/durable payload+version/source/ownerIdentity，排除 transactionId/tick/epoch/revision/policy digest/bundle ID；lowlevel digest 绑定 kind/source/canonical postings/受控 lowlevelSource/durable payload（facts 不足 → null=non-rearmable）；child contract 构建后重算并与 capability 绑定值比较（资源/数量/room/源目标/action kind/adapter 语义/structure/durable payload 变化拒绝；相同 Game 动作不同 child ID digest 一致；policy revision 变化重新授权允许；owner 变化默认拒绝）
- [x] 23.6 capability 与 bundle/intent 原子接管：authorizeTreasuryActionContract 验证 capability + 重算 retry semantic digest；bundle 私有 record 绑定 capability identity/lineage digest/child ID/retry semantic digest/parent identity；授权失败 capability 不消费、lineage 保持 ready；executePreparedAction tr1_ 路径：capability 只读验证 → lineage child_intent_pending → intent 写入（带 bindingDigest）→ read-back 一致 → consume capability → lineage child_active（current 推进、generation+1）→ callback；任一步失败 callback 零调用（回滚或 staged 恢复）；beginTick 恢复 child_intent_pending（intent 缺失/一致 not_started → 回滚 rearm_ready + 释放 intent；不一致 → forensic）；lineage 接管写失败 intent 不可执行
- [x] 23.7 parent 相反 proof 与 child 占用检查（attemptOccupancy.ts）：parent（lineage 健康/record 存在/not-executed proof 完整/authority 释放/marker 清理/pending 完成/receipt store 健康/无 committed receipt/无 committed tombstone/无 resolving committed/无 identity 冲突/class 允许/lowlevelSource 完整/retry facts 完整）；同时 final not-executed + committed → proof_conflict（零 capability、零 mutation、不删 proof、fail closed）；child 占用（receipt/tombstone/intent/quarantine/authorization-fault/write-fault marker/prepared handle/authorization bundle/其它 lineage 索引）任一 → child_identity_occupied 零签发；store unhealthy → 零 capability/零 mutation/明确诊断
- [x] 23.8 tombstone retention 与 lineage replacement 联动（resolutionStore）：final not-executed 驱逐资格 = lineage replacement 已持久化验证 + 状态允许 + release/marker/pending 完成 + 无 conflict（O(1) 索引查询，不扫描全部 lineage）；pending（publication/release/marker/conflict/unhealthy/forensic/lineage 不完整/child 接管未完成）→ pin 永不驱逐；retention 删除同步 pending 索引但不删 lineage 权威；Round 16 旧 tombstone backfill（rearmable/non-rearmable/forensic 隔离/容量不足 pin）；non-rearmable retired lineage永久阻断 parent 但拒绝 rearm
- [x] 23.9 write-fault marker v2（markerAttemptIdentity.ts + writeFault）：marker 新增 authorityClass/lowlevelSource/lineageBindingDigest/attemptGeneration/markerVersion；清除使用完整 class-aware attempt relation（runtime/migrated-lowlevel 互不清除、parent/child 互不清除、不同 lineage 互不清除、modern 不被 lowlevel/legacy 清除、legacy 不自动清 modern）；malformed marker rearm 结构化拒绝不抛异常；immediate not-executed 检查 marker 清除结果（成功才 pending-release 完成 + rearm-ready + capability 可申请；conflict/insufficient → resolved_pending_cleanup 状态、tombstone/pending/索引保留、零 capability）
- [x] 23.10 receipt proof class 三级（receipts v7）：identity-bound（完整 modern identity、禁 lowlevelSource）/ lowlevel（digest+durable identity+受控 lowlevelSource+可选 binding、禁 contract/cohort）/ legacy（无身份、只 replay blocker）；v6 迁移（modern 无 source → identity-bound、有合法 source → lowlevel、legacy → legacy、矛盾 fail closed）；lookup/commit/refresh/finalized proof/committed verifier/migration/cleanup/metrics 更新；不同 class 不互相释放 authority；runtime-lowlevel receipt 不证明 migrated-lowlevel authority；合法 proof 全部保留 replay blocker 语义
- [x] 23.11 lineage binding 进入 durable proof 链（lineageBinding.ts）：binding digest（lineageId+generation+parent/child ID）进入 child intent/quarantine/authorization-fault/write-fault marker/receipt/tombstone/reconciliation capability/committed verifier/attempt identity relation；tr1_ attempt 缺 binding → unhealthy 或 forensic；不同 lineage/generation/parent proof 互不证明；同 child ID 不同 lineage digest → identity conflict；initial attempt 不得携带
- [x] 23.12 确定性测试：tombstone retention 与永久 retirement（超龄驱逐后 parent 仍拒绝/无 replacement 不驱逐/marker pending 不驱逐/release pending 不驱逐/lineage full 不释放 authority/驱逐后仍可签发 capability/non-rearmable 永久阻断/pending tombstone 不因容量删除/store 异常 pin/retention 删 pending 索引不删 lineage）；opaque capability（签发/JSON 失效/伪造失败/跨 tick/跨 service/重复消费/跨 parent/跨 child/同 tick 幂等/global reset 重签发 child 一致/接管后不可重签/revision 变化失效）；tr1_ 门禁（手工构造拒绝/test helper ID 无 capability 拒绝/compat 路径拒绝/initial tr1_ 拒绝/正确 capability 通过/错误 child 拒绝/已消费拒绝/架构扫描）；retry semantic identity（资源/数量/room/target/action kind/adapter 语义/structure/durable payload 变化拒绝；相同动作不同 ID 一致；policy 重新授权允许；owner 变化拒绝；lowlevel facts 不足 non-rearmable）；parent proof 与 child 占用（committed receipt/tombstone 冲突/receipt fatal/parent marker/authority/pending 清理/child 各 store 占用/另一 lineage 占用/prepared handle/v5 缺 lowlevel proof）；capability 接管（授权失败不消费/bundle 未执行 tick 结束重签发/bundle-to-tentative 失败恢复/intent 写失败零 callback/lineage 接管失败 intent 不可执行/global reset 补完成或回滚/read-back 一致后 callback/callback 后不可重签/intent 携带 binding/quarantine-receipt-tombstone 继承 binding）；marker v2（runtime/migrated 互不清除/parent-child/不同 lineage/modern 被 lowlevel/legacy 被 modern/matching 清除/conflict 保留 pending/immediate 清理失败不返回 rearm-ready/malformed 结构化拒绝/读取嵌套 identity 不可改 Memory）；receipt proof class（v6 迁移三级/identity-bound 带 source unhealthy/lowlevel 缺 source unhealthy/legacy 带身份 unhealthy/跨 class 不释放 authority/runtime 不证明 migrated/合法 proof 仍 replay blocker）；lineage 链（A→B→C 推进同 record/旧 ID 不可重用/每代一个 child/committed 后不再签发/不同 root 不同 lineage/global reset 索引重建/索引损坏 unhealthy/alias 隔离/深冻结/满载推进可行新 root 拒绝/普通 retention 永不删 lineage）；回归（Round 16 cohesion/store_unhealthy/状态矩阵/capability 顺序、Round 15 committed 三方 proof、Round 13 receipt 迁移、normal contract→bundle→intent→OK→commit、non-OK→abort、unknown→quarantine、production writer 边界扫描、真实 writer diff 为空、query/readiness 零 Game 写入）
- [x] 23.13 operation-count fixture：retired root prepare 门禁零全扫；child capability issuance 零全扫；child 占用检查不全扫 receipt/tombstone；tombstone retention 只做必要索引查询；chain generation 推进不新增 store slot；后续 tick 不重复全扫 lineage store
- [x] 23.14 验证与证据：typecheck / build / treasury 定向 Jest / 全量 Jest / verify-jest-budget 真实通过；evidence（round17-durable-attempt-lineage-rearm-capability-local-validation.md，含起始/最终 HEAD、commit 列表、lineage 状态图、root/current/next 索引、hard capacity 与满载行为、staged publication 顺序、tombstone retention 资格、rearm capability 生命周期、tr1_ 门禁、retry digest 包含/排除事实、capability→bundle→intent 接管顺序、marker v2 身份、receipt 新 proof 等级、store 版本与迁移、operation-count、真实命令与 suite/test 数字、未部署/未合并/未接真实 writer、GitHub 无 CI 本地验证、Screeps hard CPU interruption 与 Memory flush 边界不保证 exactly-once）；预算锚点更新（requiredBaselineCommit 指向含全部实现与测试的前置 commit）

## 24. Round 18 — Lineage Handoff Atomicity & Generation-Proof Closure

- [x] 24.1 lineage replacement publication 原子性（faultResolution 重排）：resolve-as-not-executed 安全顺序改为 prevalidate → resolution slot 预检 → lineage 容量与 retry facts 预检 → **lineage retirement candidate 持久化并 read-back（含 identity 与 tombstone/authority 匹配验证）** → consume capability → final tombstone → 释放 quarantine/intent → class-aware 清 marker → 三段完成推进 rearm-ready/non-rearmable；publication 写入/read-back/索引/identity 任一失败 → intent 保留、quarantine 保留、marker 保留、pending-release 索引保留、final tombstone 未写则不写、返回 lineage_publication_pending（beginTick 下一轮从保留 authority 重建完整 retry facts，不退化为只能 non-rearmable backfill）；三段（publication/release/marker）全部 verified 才移除 pending-release 索引、获得 retention 驱逐资格、进入 rearm-ready
- [x] 24.2 child handoff 状态机（lineageHandoff.ts 单一权威）：capability_issued 持久化 handoff facts（child ID/parent/target generation/pendingBindingDigest/retry semantic/authority class/owner/lowlevel/expected current identity）；consume 删除 skip-all-revision 宽松语义——严格验证 capability 对象身份/未消费/service/tick/lineage 存在/lineageId/child/generation/binding 一致/lineage 处于预期 handoff 状态/recordRevision 等于 capability 允许的明确 revision/retry semantic 一致；tr1_ 执行顺序：capability 验证（state=capability_issued）→ lineage child_intent_pending → intent 写入（携带 lineageId/generation/parent/binding）→ read-back（含 lineage proof）→ consume（state=child_intent_pending、revision=issue+1）→ intent execution-started（executing）→ lineage child_active（current/generation/binding 推进）→ callback；global reset 恢复矩阵：capability_issued → rearm_ready（child ID 稳定）；child_intent_pending + intent 缺失 → 回滚；+ 一致 ready intent → 释放并回滚（不 forensic）；+ binding/generation 冲突 → forensic；+ intent executing 或更后/或 quarantine 已接管（binding 匹配）→ 前向补完成 child_active；不产生第二 child、同 generation child ID 一致
- [x] 24.3 child 结果分支闭合（facade）：pre-callback 任意失败（intent 写/read-back/consume/execution-started/armed 推进/retry semantic 重验/source 不匹配/lineage unhealthy）callback 调用数 0、回滚到 rearm-ready 或保留明确 handoff-pending 供 beginTick 恢复；child 明确 non-OK 且 abort 确认 → 同步 child generation not-executed retirement（lineage child_active→retiring publication → final tombstone（tr1_ proof）→ 释放 intent → 清 marker → rearm-ready；publication 失败保留 intent 事实、lineage 不丢失，quarantine 路径收敛）；non-OK abort 失败 → quarantine（不得当 not-executed 完成）；A→B non-OK → C 可继续；child commit 成功 → chain_committed（receipt 先行、携带 lineage proof；lineage 终态更新失败不忽略返回值——intent 保留、返回 executed_unsettled、beginTick 按 matching receipt 补完成；receipt 与 lineage generation 冲突不补完成）；resolution-as-committed 后 lineage 最终 chain-completed；chain-committed 后不得再签发下一代 capability
- [x] 24.4 generation proof 进统一 durable identity（lineageAttemptProof.ts）：单一版本化 lineage attempt proof 视图（lineageId/generation/parentTransactionId/bindingDigest/authorityClass）；统一 durable identity 计算：tr1_ 携带完整 lineage proof、initial 完全不携带、单侧缺失/不同 lineage/generation/parent/binding → conflict 或 insufficient（不得 match）；统一 attempt relation 比较 proof class/digest/contract/cohort/durable identity/lowlevel source/lineageId/generation/binding/parent-current 关系；传播到 action contract、authorization bundle 私有 record、intent、quarantine、authorization-fault、write-fault marker、receipt、resolution tombstone、reconciliation capability、finalized proof、committed 三方 verifier、same-ID 幂等、intent→quarantine 转移、lineage retirement proof、child occupancy
- [x] 24.5 intent/quarantine/publication proof 矩阵（store 版本升级与迁移）：is rearm attempt → lineage proof 全部必填；is initial → 全部禁止（不只检查可选 digest 格式）；same-ID 幂等比较 lineage ID/generation/binding/parent/retry semantic（同 ID 不同 proof 永远 conflict）；durable publication read-back 比较全部 lineage proof（检测写入后 binding 删除/generation 篡改/lineage ID 变化）；intent→quarantine 转移前验证 class/immutable identity/outcome 相容/lineage ID/generation/binding/parent/retry semantic（任一不一致 intent 保留、quarantine 不接管、callback 不重放）；tr1_ 旧 entry 缺 proof → 从 lineage 安全补全或 forensic/store unhealthy（不猜测）；non-tr1_ 携带 lineage 字段 → unhealthy
- [x] 24.6 lineage store 索引与写入完整性（lineageIndexIntegrity.ts + store v2）：维护并验证 lineageId/root/current/next 四索引全 O(1)（lineageId read 不再扫描 entries）；load 与每次写入前检查 lineageId/root/current/next 全局唯一与跨索引无冲突（root≠他 record current/next、current≠他 record root/next、next≠他 record root/current/next、record 内组合符合状态语义）——冲突 → 整个 store unhealthy、不由 Map.set 静默覆盖、不自动删除；Memory 写入成功但索引更新失败 → 完整回滚或 runtime fatal；publication read-back 比较全部安全关键字段（root/current identity、action kind、adapter stable identity、owner、generation、state、resolution state、child ID、retry semantic、class/source、binding、retirement facts、revision、protocol version）
- [x] 24.7 lineage 状态机收紧（exact idempotence + transition-specific mutation）：candidate 与 existing 完整一致且 revision 一致 → 真正 idempotent（revision 不 +1）；合法推进 revision 严格 +1；同状态仅允许明确声明的进度字段变化；每条转换建立允许变化集合并冻结 lineageId/root ID/root identity/authority class/lowlevel source/action kind/adapter stable semantic identity/owner/retry semantic 协议版本/created tick；current identity/generation/binding 只在接管转换同时变化；retirement 按 generation 重置（进入新 generation 不继承上一代 retirement 完成标志；retention 查询确认 retirement facts 属于目标 tombstone 的 generation）；updatedAt 不回退、generation 不回退、revision 不跳跃
- [x] 24.8 per-generation tombstone replacement（lineageGenerationRetirement.ts + resolutions v7）：retention 驱逐资格按"该具体 attempt generation"判定——verdict ∈ {replacement_match, replacement_pending, replacement_conflict, replacement_missing, store_unhealthy}；match 条件：lineage ID/generation/transaction ID（generation-addressable child ID v2）/attempt identity（当前代完整比较；历史代经 ID 协议 + 状态机链证明）/binding（按 (lineageId,generation) 重算比较）/proof class/resolution=not-executed/retirement 三段完成；pending/conflict/unhealthy/missing → pin；上一代 retirement 全 true 不得授权当前代驱逐；A→B→C 后 A/B tombstone 均可独立回收、旧 ID 仍不可直接执行、单 chain 多代 retry 不线性耗尽 Resolution store、active lineage entryCount 不随 generation 增长
- [x] 24.9 rearm child ID 协议 v2（generation-addressable）：child ID = tr1_<lineageId16>_<generation6hex>_<checksum8>（绑定 root transaction ID 的 checksum）；同 lineage+generation 确定性唯一、O(1) 从 ID 解析 (lineageId, generation)、O(1) 重算验证；旧 tr1_ v1 ID 继续安全门禁（不可解析 → legacy 隔离/pin，不猜测 generation）；单一 namespace gate 管理（前缀不变）；store v1→v2 迁移（capability_issued/child_intent_pending 回退清除 v1 next-child；其余状态携带保留；v1 child 不可 generation 寻址 → 相关 tombstone 永久 pin）
- [x] 24.10 terminal lineage 压缩与退休摘要（lineageRetirementSummary.ts）：chain_committed（无 intent/quarantine/marker/pending handoff/pending finalization、root 摘要可持久化 read-back）与 non_rearmable_retired（三段完成、无 pending authority/marker）可从 active store 压缩；forensic 不自动压缩；retirement summary 独立 store（精确权威：永久阻止 root ID 重用、证明终态、绑定 root identity 与 lineageId、区分 committed/non-rearmable、prepare O(1) 查询、不依赖普通 receipt/tombstone retention）；summary 独立硬容量（满载不删除旧 summary、不压缩 active record、新 chain 容量门禁 fail closed）；压缩成功释放 active slot；Root ID 仍永久拒绝；summary 读取 O(1)、输入 alias 不污染 Memory、store 损坏 prepare fail closed
- [x] 24.11 rearm preflight 完整 proof 与 store health：capability 签发前检查 lineage/retirement summary/receipt/resolution/intent/quarantine/authorization-fault/write-fault marker 全部 store 健康（任一 unhealthy → 零 capability、零 lineage mutation、零 callback）；tombstone 存在时验证 final not-executed tombstone ↔ lineage current generation retirement proof 完整匹配（attempt identity/proof class/lowlevel source/lineage/generation/binding）；tombstone 已合法驱逐 → 由 lineage generation retirement proof 或 terminal summary 证明（不因"没有 tombstone"默认可信）；相反 proof（committed receipt/tombstone/resolving/identity 冲突）继续阻断；child 占用检查区分 absent/occupied/store unhealthy（损坏 store 不当 absent）
- [x] 24.12 稳定 adapter retry semantic contract（adapterRetrySemantics.ts + retrySemanticIdentity v2）：adapter 显式声明版本化 retry facts（从 canonical frozen args 派生、受 shape validation/canonical encoding/大小上限/异常边界约束、与 durableFacts 职责分离、覆盖全部改变真实 Game API 调用语义的参数）；retry digest 绑定 action kind/adapter version/stable semantic identity/retry semantic protocol/canonical retry facts/postings/structure/owner/source，移除 per-global registration sequence/global 对象身份/函数源码字符串；相同 stable 语义与 retry facts 跨 global reset/注册顺序变化 digest 相同；adapter 未实现 retry facts → 正常执行、not-executed 后 non-rearmable（不猜测）；retry facts 抛错/超限 → non-rearmable/fail closed；改变一个真实 Game 参数 → digest 变化；policy revision 变化 → digest 不变但重新授权；durable payload 相同而 retry facts 不同 → digest 不同；retry semantic 版本变化 → 旧 capability 拒绝
- [x] 24.13 contract source 单一权威：source 在 contract build 时确定并进入 contract identity/retry semantic/durable intent/authorization context/reconciliation facts；authorization 不写死 action-contract（使用 contract source 重算）；execution request 不得覆盖已授权 source（不同 → callback 前拒绝）；parent/child source 变化 → retry 拒绝
- [x] 24.14 确定性测试（round18）：lineage publication 原子性（tombstone 成功/candidate 写失败/read-back 失败/索引失败/identity 冲突 → authority+marker+pending 索引保留；pending 不返回 rearm-ready、不移除 pending-release；下一 beginTick 重试完整 publication；release 完成但 marker 失败 → cleanup-pending；三段成功才完成）；handoff reset 窗口（capability_issued 后 reset → ready 且 child ID 稳定；handoff+intent 不存在 → 回滚；+matching ready intent → 释放回滚不 forensic；+binding 不同/generation 不同 → forensic；intent 写入后 consume 前 reset → 回滚；consume 后 armed 前故障 → 前向恢复不产生第二 child；consume 遇非预期 revision/state → 拒绝且 callback 零调用；不再有 skip-all-revision 旁路）；pre-callback failure（intent read-back 失败/armed 更新失败/execution-started 失败/retry semantic 重验失败/source 不匹配 → callback 零调用且 lineage 可恢复）；child non-OK（non-OK+abort → 不停留 child-active、形成 generation not-executed proof、当前 child 进 rearm-ready、下一代 capability 可签发、下一代 child ID 不同；publication 失败 → intent 保留 lineage 不丢失；abort 失败 → quarantine；A→B non-OK → C 可执行）；child commit（成功 → chain-committed；receipt 成功但 chain-comitted 写失败 → executed-unsettled + intent/pending 保留；beginTick 按 matching receipt 补完成；receipt 与 generation 冲突不补完成；commit 后不得签发下一代；更新结果不忽略）；lineage durable proof（tr1 intent/quarantine 缺 binding/generation → store unhealthy；tr1 receipt/tombstone 缺 proof → 不释放 authority；initial 携带 → unhealthy/拒绝；same-ID 不同 lineage/generation/binding → conflict；parent proof 不能证明 child；generation N 不能证明 N+1；finalized proof 传播；reconciliation capability 绑定）；intent→quarantine（binding 完全相同转移成功；丢失/篡改/generation/parent 改变 → intent 保留；same-ID 幂等比较 proof）；index 完整性（duplicate lineageId/current/next、跨索引冲突 → unhealthy；写入冲突原 store 不变；lineageId lookup O(1)；Map 不静默覆盖；索引故障回滚或 fatal）；transition（exact 相同写入 idempotent 且 revision 不增；合法转换严格 +1；同 state 改 actionKind/adapter identity/owner → 拒绝；非接管转换改 current/binding → 拒绝；updatedAt 回退拒绝；新 generation retirement 重置；旧代布尔不能授权当前代驱逐）；per-generation retention（A/B replacement 完成 → 可驱逐；A→B→C 后 A/B 均可独立驱逐；B pending → pin；generation/binding 不匹配 → pin；store unhealthy → pin；单 chain 300 代不永久满载；entryCount 不随 generation 增长）；terminal 压缩（committed 可压缩、slot 释放、root 永久拒绝；non-rearmable 可压缩；pending handoff/marker 不可压缩；forensic 不普通压缩；summary 满载不删旧、active 保持、新 root 拒绝；O(1) 读取；alias 不污染；损坏 fail closed）；rearm preflight（resolution/auth-fault/marker store 损坏 → 零 capability；tombstone 与 lineage identity/generation 冲突 → 零 capability；tombstone 合法驱逐 + generation proof → 可签发；occupancy store 损坏不当 absent；parent committed 冲突继续阻断）；retry semantic 稳定性（注册顺序变化 digest 相同；global reset digest 相同；版本变化旧 capability 拒绝；未实现 retry facts → non-rearmable；抛错/超限 fail closed；真实参数变化 digest 变化；policy revision 不变 digest；payload 相同 facts 不同 → digest 不同；source 变化拒绝）；contract source（非默认 source parent/child 授权通过；authorization 不强制改写；execution 覆盖 → callback 前拒绝；source 进 digest 与 retry semantic；不一致不消费）；回归（Round 17 capability 防伪/tr1 门禁/parent conflict/receipt v7 migration、Round 16 cohesion、Round 15 三方 proof、Round 13 迁移、initial 全链路、production writer 扫描、真实 writer diff 空、query/readiness 零写入）
- [x] 24.15 operation-count fixture：lineageId read 不全扫；单个 handoff recovery 不全扫；单条 tombstone replacement 查询 O(1)；A→B→C 推进不增加 active entryCount；terminal 压缩释放 active slot；空闲 beginTick O(1)
- [x] 24.16 验证与证据：typecheck / build / treasury 定向 Jest / 全量 Jest / verify-jest-budget 真实通过；evidence（round18-lineage-handoff-generation-proof-local-validation.md，含起始/最终 HEAD、commit 列表、staged 顺序、handoff 状态图与全部中断窗口、child non-OK/committed 状态图、generation proof 字段语义、store 版本与迁移表、index 完整性矩阵、transition 允许字段矩阵、tombstone replacement verdict、多代退休/压缩方案、active 与 summary 容量、adapter retry semantic 协议、source 绑定、operation-count、实际命令与数字、未部署/未合并/未接真实 writer、CI 实际状态、Screeps hard CPU interruption 边界）；预算锚点更新（requiredBaselineCommit 指向包含全部实现与测试、位于 budget 提交之前的 commit）
