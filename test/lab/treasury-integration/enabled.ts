/** Continuation 0002 preparation: the lab-ti1-0001 experiment is closed and its
 * enabled=true is history, NOT an authorization. Back to the default disabled
 * state for the new preparation round; it is flipped to true again only in the
 * same bound-source change that fixes the new final T on the new world's paused
 * T0. It is not a runtime Memory override and there is no environment-variable
 * bypass. */
export const TREASURY_INTEGRATION_ENABLED: boolean = false;
