/** Offline delivery is unarmed. The verifying Agent may change this constant
 * ONLY in the same bound-source change that prepares the authorized NEW local
 * treasury integration experiment. It is not a runtime Memory override. */
export const TREASURY_INTEGRATION_ENABLED: boolean = false;
